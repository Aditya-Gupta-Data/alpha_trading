# MODULES.md — Master Component Index

One-line-plus purpose for every file that matters. If a file isn't listed
here, it's either generated (`__pycache__/`), static assets, or trivial. For
system flow between these, see `ARCHITECTURE.md`.

**Maintenance rule (2026-07-10):** any commit that adds, moves, or
repurposes a module updates this file IN THE SAME COMMIT. This index is
the first stop for every code-review/search agent — point them here
instead of letting them grep the tree from scratch; a stale index is what
makes reviews expensive.

## Department map (the Manager registry) — see `ARCHITECTURE.md` for the plain-English version

Every module belongs to ONE department; to change a department's behavior,
approach its **Manager** — don't dig through files. (Manager-of-record semantics
and each department's design role live in `ARCHITECTURE.md`; this table is the
navigation map — department → the file-sections below.)

| # | Department | Manager (the seam you approach) | The file sections below |
|---|---|---|---|
| 1 | **Data** (market data in) | `dhan_guard.SafeDhanClient` (+ `token_provider`; `market_snapshot` for shared marks) | Data layer · Ingestion |
| 2 | **Decision** (the live engine) | `options_proposer.run_headless` (+ `decide_pending`; composed by `master_scheduler`/`market_loop`) | Live engine · Strategy |
| 3 | **Risk & Capital** (gatekeeper) | `portfolio_manager` (entry/margin) + `plan_tracker` (THE exit/settlement path) | Risk & capital |
| 4 | **Memory & Learning** (ledger+brain) | `journal` (truth) + `brain_map` (learning); nightly `sleep_phase`; `tuner` (weights) | Knowledge graph · Memory |
| 5 | **Validation** (proving court) | `validation/registry` (lifecycle) + `validation/stat_gates` (the one rulebook) | Validation · Discovery |
| 6 | **Reporting** (announcer) | `notifier.fire_broadcast` (the one Discord door) | Reporting & advisory |
| 7 | **Interfaces** (front doors) | `api_server` (gateway) → `api` / `discord_bot` | Interfaces |
| 8 | **Analysis** (the research desk; sits between 1 and 2 in the data flow — numbered 8 so refs to 1–7 stay stable) | `analysis/regime_filters.advise()` — the ONE seam an analysis verdict reaches a live decision through (composed in `market_loop`, honored via `build_proposal(advisory=)`); risk-REDUCING authority only until Dept 5 grants more | Analysis (top-down sectoral) |

## Data layer (market data)

| File | Purpose |
|---|---|
| `src/dhan_client.py` | THE market-data source. DhanHQ SDK wrapper: `SECURITY_ID_MAP` (verified against Dhan's scrip master), `get_daily_ohlc`, `get_ohlc_since`, `get_live_price`, `get_quote`, `get_daily_closes`, `get_option_chain`/`get_expiry_list`. Data-only — no order methods. |
| `src/dhan_guard.py` | `SafeDhanClient` — hardened, audited access to the same market-data endpoints: classified DH-9xx failures on `last_error` + rolling `audit` (auth vs data outages distinguishable; **DH-905 is an INPUT error, not auth** — corrected 2026-07-20, it was paging a dead-token alarm for a merely malformed request. `auth_failures()` = DH-901/906, `input_failures()` = DH-905), single rate-limit retry, `strict=True` raise mode (tests only). Freshness guard voids >60s-old INDEX quotes mid-session only (equities idle legitimately; >3h "age" = miszoned stamp, ignored). Re-exports `dhan_client.unwrap_payload`. Same return shapes/empty states as `dhan_client`. |
| `src/token_provider.py` | The self-healing token seam (Issue 5 fix): `get_token()` re-reads `.env` on mtime change so long-running processes pick up an external renewal WITHOUT restart; `set_override()` for tests/push. `dhan_client._get_client` consumes it — the ONLY Dhan SDK construction site. |
| `src/market_snapshot.py` | Phase 8: the engine's published market read-model at `data/market_snapshot.json` — `live_cycle(publish_snapshot=True)` writes spots + every open position's mark atomically each cycle; viewers (`portfolio_report.get_live_marks`, the dashboard) READ it instead of re-fetching quotes, keeping the live loop the single Dhan consumer (decision #48). `read(max_age_seconds=...)` treats stale/corrupt/missing as None; write failures never touch the loop. |
| `src/data_fetcher.py` | Thin re-export of `dhan_client.get_quote` — kept for the original `get_quote(ticker)` contract older callers use. |
| `src/exposure_gate.py` | Decision #68 structural exposure cap + trend-flip exit advisory. `gate_entry` — ONE open spread per underlying+direction (reads `positions.active_positions`, fail-OPEN, sandbox-exempt, runs BEFORE the margin gate); blocks ledgered to `logs/exposure_blocks.jsonl` + at most one Discord note per (ticker, direction) per IST day (the ledger is the memory). `trend_flip_advisory`/`TrendFlipRegistry` — SMA-cross contradiction vs open directional positions, one de-duped card per flip (fires on first observation too, so a restart into a contradicted book alerts once); condors never fire. Binary verdicts only (#63), advisory-only exits (#11/#41), writes nothing but its ledger. |
| `src/live_bridge.py` | Phase 6H live market-hour adapter: `fetch_live_market_state` (drop-in `fetch_fn=` for `run_market_loop` — live spot appended to the daily-close trend read), `parse_packet`/`CandleAggregator` (quote snapshots → 15-min OHLC), `evaluate_open_positions`/`live_cycle` (real-time advisory exit signals on open spreads via `plan_tracker`'s pure helpers, de-duped Discord alerts; `flip_registry=` kwarg arms decision #68's trend-flip exit advisory — None, the default, is a byte-identical no-op). READ-ONLY on all trade state (decision #41) with ONE sanctioned amendment (#69): with `square_off_fn` armed (the production daemon, config-gated via `"intraday_profit_take"`, default ON), a profit-take signal on an approved spread is EXECUTED intraday — `intraday_square_off` fetches real chain quotes per leg (`_leg_quotes_for`; any missing quote → advisory fallback) and calls the tracker's one settlement path. `square_off_fn=None` (every legacy caller/test) stays byte-identical advisory. Daemon: `python3 -m src.live_bridge`. |
| `src/indicators.py` | Pure-Python SMA and Wilder's RSI, no dependencies. |

## Knowledge graph

| File | Purpose |
|---|---|
| `src/graph_engine.py` | Phase 6C reasoning layer: `ensure_schema` (creates `graph_edges` table + temporal columns), `add_edge` (idempotent writer — stamps `valid_from`, clears `invalid_at` on reinforce), `GraphEngine` (loads ACTIVE edges only — `WHERE invalid_at IS NULL` — into a `networkx.DiGraph` for 2-hop BFS context queries). |
| `src/regime.py` | Regime-Aware Memory vocabulary (roadmap #4): `vix_band` (low <13 / mid 13-16 / high >16 — the single source; evolution + planner boundaries align), `regime_for(view, vix)` attached to journal entries at creation, `encode_for_model` for the skeptic's v2 feature contract, and the as-of-date historical backfill (`python3 -m src.regime backfill --db …`, bars-cache-driven, idempotent, never guesses — untaggable rows get 'unknown'). Stored as additive nullable `regime_trend`/`regime_vix` columns on `outcomes` + `simulated_trades`; queried via `query_similar_events(tags, regime=…)`'s `in_regime` block. |
| `src/brain_map.py` | Phase 6 relational event-pattern memory: sqlite `events`/`outcomes`/`event_outcome_link` at `data/brain_map.db` (+ additive regime/post_mortem columns), `record_event`/`record_outcome`/`link_event_outcome`, `query_similar_events(tags, regime=…)`, `ingest_existing` backfill CLI, `_normalize_tag` (THE tag normalizer everything clusters on). |
| `src/local_parser.py` | Phase 10B local-Ollama text→JSON extractors: `LocalExtractor` (event frames, `chat_json`, causal triples) — ONLY network I/O is localhost Ollama, zero market-data imports (decision #30), every failure returns None quietly (offline logged once per process).  NEW 2026-07-19: `unload()` — flushes the model from Ollama's memory NOW (native `/api/generate` keep_alive=0, the documented mechanism); batch jobs (annual_report_analyzer) call it after each document; never raises. |
| `src/text_intelligence.py` | Decision #74: THE text→structured-JSON backend MANAGER for the Data Department — one seam so the LLM backend is a config choice (`text_intelligence_backend`), not a code change. Every backend exposes the LocalExtractor-shaped contract (`chat_json(system,user)→dict\|None`, `is_reachable`, `base_url`/`model`), so `news_parser` is byte-identical whichever is chosen. Backends: `ollama` (LocalExtractor, DEFAULT — unchanged, local only) and `claude` (`ClaudeExtractor` — raw httpx to the Anthropic Messages API, the cloud path the VM can reach; no `anthropic` SDK, matching the Gemini/Ollama httpx convention; model config `text_intelligence_model` default `claude-opus-4-8`, set `claude-haiku-4-5` for cheap bulk). SINGLE mode = `get_extractor().chat_json(...)`; BATCH mode = `submit_batch`/`poll_batch`/`fetch_batch_results` drive Anthropic's Message Batches API for Phase-1 historical backfill. Guards: `within_daily_budget`/`record_call` (cap `text_intelligence_daily_call_cap`, ledger `logs/text_intel_calls.jsonl`) + `already_processed`/`mark_processed` (hash ledger — incremental daily re-parses only NEW text). BILLING NOTE: Claude API bills API credits (NOT the Max/Pro subscription); default stays `ollama` until credits confirmed. Every network seam injectable (`post_fn`/`get_fn`); fail-open throughout. NEW 2026-07-19: `gemini` backend (`GeminiExtractor`, raw httpx — no SDK, house convention; key in the x-goog-api-key HEADER never the URL; `gemini-flash-lite-latest` ALIAS default per the pinned-names-rot lesson, `gemini_model` config escalates) — the hybrid pipeline's SYNTHESIS layer: measured 2026-07-19 breaking the eMudhra synthesis wall on the first flash-lite call (summed 124.62+476.38=₹601 Mn capitalized development, verbatim quote, correct flatters-profit reasoning) where three local 3B-class models failed. |
| `src/sleep_phase.py` | Off-hours consolidation batch (cron 20:00 IST): decay sweep, episodic replay/causal-link writing via local Ollama (VM skips LLM tasks silently — it runs no Ollama by design), evolution Task E hook. |
| `src/ingestion/macro_tracker.py` | Phase 7: Crude / Gold-India / Gold-World / USDINR → SHORT(5-bar)/MEDIUM(21)/LONG(126) directional matrix. Dhan live path uses VERIFIED ids from `config/macro_securities.json` only (never guessed), fails open per-metric/per-horizon to hand-editable `data/macro_snapshot.json`, else "unknown". `INDEX_IMPACT_WEIGHTS` maps each permutation onto NIFTY 50 / NIFTY BANK bias; gold India-vs-World divergence flags currency-driven moves. |
| `src/ingestion/news_parser.py` | Phase 7: headline/social text → strict 5-key signal frame {target_entity, event_classification, directional_bias, horizon_impact, confidence_score} via `LocalExtractor`; `canonicalize_entity` is the shared entity vocabulary (Brent→CRUDE, NIFTY→"NIFTY 50", strips .NS). |
| `src/ingestion/deals_tracker.py` | Phase 8 (decision #60): EOD NSE bulk & block deals → per-ticker smart-money footprint {net_qty, net_value_rs, buy_deals, sell_deals, block_deal, marquee_names, marquee_net}. Live NSE path (cookie handshake) fails open to hand-editable `data/bulk_deals_snapshot.json`, else "none"; net-direction not raw count; marquee tagging from `config/deals_watchlist.json`. Writes `data/bulk_deals.json` (daily aggregate) AND appends `data/deals_history.jsonl` (raw per-deal ledger, idempotent per day, via `append_raw_deals`/`read_deal_history`) — the substrate the entity-affinity layer learns from; plus a per-day census row (tape-quality telemetry; near-dup alias candidates for human review fire a REAL-TIME Discord card via `_notify_review_items` — owner directive 2026-07-16: review flags never sit log-only — de-duped per group via the append-only `logs/census_alerts.jsonl` ledger, fail-open, and a failed send does NOT mark groups seen so they re-announce when Discord is back) and the raw NSE payload hashed into the lake. `--backfill YYYY-MM-DD` crawls NSE's historical archives (60-day windows, throttled, raw windows archived, per-row report-date parsing across eras, idempotent per day) — run from the Mac. `load_deals()` reads the aggregate back for consumers. Advisory-only, not yet wired into forecasts. |
| `src/lake.py` | Phase 0 (holy-grail plan): the cold store beside brain_map.db — date-partitioned gz-JSONL under `data/lake/<dataset>/date=YYYY-MM-DD/`, atomic writes, gzip-member appends (intraday taps), `scan`/`read_day` readers, `archive_blob` (sha256; upstream revisions become loud rev- files). ONLY ingestion writes; greppable end-to-end (zcat); no Parquet/DuckDB/new datastore. HOT-state rules (#19/#25) unchanged. |
| `src/ingestion/chain_archiver.py` | Phase 0: post-close (15:40 IST Mon-Fri) capture of the nearest 4 NIFTY/BANKNIFTY expiries (full `oc` + spot + VIX) → `data/lake/chains/` — historical chains are unbuyable (#36); throttled, fail-open per expiry, weekend no-op, heartbeat-monitored. |
| `src/ingestion/daily_archiver.py` | Phase 0: daily 19:45 IST snapshot of the perishable artifacts (news_sentiment.json, the macro matrix) into the lake (`news_daily/`, `macro_daily/`) — the load-bearing prerequisite for every cross-layer join; independent + fail-open per artifact. |
| `src/ingestion/rss_ingester.py` | Decision #75: official-RSS news pipeline. Reads PUBLISHERS' OWN RSS feeds (`config/rss_feeds.json` — Moneycontrol/ET/BS, hand-editable, per-feed `sector`) via a User-Agent'd stdlib urllib+`xml.etree` fetch (the `news_processor` pattern — NO `feedparser` dep, no scraping ⇒ no IP ban), dedups against a hash ledger (`logs/rss_seen.jsonl`), and classifies ONLY NEW headlines through the Text Intelligence Manager (#74). THE cron seam is `run_daily_pull()` — fetch→dedup→classify→append `data/rss_signals.jsonl` + lake `rss/`; fail-open per feed/end-to-end. Sector comes from the feed (trusted); the LLM supplies the entity/impact via `news_parser`'s 5-key frame. Pins the CHEAP model (`rss_model` default `claude-haiku-4-5`); `rss_backend` INHERITS the global `text_intelligence_backend` (default ollama) so it's COST-SAFE by default — on the VM classification skips (zero API spend) until the cloud backend is enabled. Doubly cost-bounded: shared daily call cap + incremental de-dup (only new headlines hit the LLM). RSS = latest items only ⇒ nightly SINGLE mode (the Batches API stays for a future historical-archive source). Advisory/capture-only, not yet wired into forecasts. CLI `python3 -m src.ingestion.rss_ingester`. |
| `src/ingestion/scrip_master.py` | The scrip-master reconciliation clerk (Dept 1, 2026-07-20 — **closes review #2's standing gap**, ARCHITECTURE.md's "until that job exists, every deploy must re-verify IDs by hand"). `SECURITY_ID_MAP`'s hard-coded broker ids silently ROT on delist/demerge — nothing crashes, we just price the WRONG instrument (ledger Issues 14/15: LTIM delisted, TATAMOTORS demerged into TMPV/TMCV, both caught by a human at deploy time). Weekly it fetches Dhan's PUBLIC scrip master (~27MB static CDN CSV, no credentials, no session — **not NSE, so it carries none of the ban risk that makes the other clerks Mac-only; this one runs anywhere**) and judges every mapped id: `ok` / `symbol_mismatch` (the dangerous one — a live id now trading as something else) / `id_not_found`. Name matching is normalized (case/space/punctuation-blind) so the master's own spellings — 'Nifty 50' for `NIFTY 50`, 'BANKNIFTY' for `NIFTY BANK` — don't false-alarm weekly. WANTED LIST (`config/scrip_wanted.json`): symbols we lack an id for get looked up by EXACT trading-symbol match (never fuzzy — a wrong id here is the very bug this prevents); GOLDBEES is the standing entry, its verification being the documented blocker on the wealth-flywheel merge. NOTHING auto-corrects — a guessed replacement id is the same bug again, so it reports and a human edits the map. HONESTY: a fetch/parse failure returns `status:"unavailable"` + SM-4xx/5xx to `logs/scrip_master.jsonl`, **never a pass** (CLI exits 1); the raw 27MB CSV is deliberately NOT archived (re-fetchable, weekly copies would bloat the lake) — the report `data/scrip_reconciliation.json` is the artifact. One de-duped Discord card per NEW problem keyed (ticker, verdict, id); a failed send doesn't mark items seen. First live run 2026-07-20: 218,714 master rows, **88/88 verified, zero drift**, and it resolved GOLDBEES = NSE_EQ id 14428. CLI `python3 -m src.ingestion.scrip_master [--json] [--quiet]`. **DARLING IDS (#83):** `build_darling_ids`/`ensure_darling_ids` — the VM desk's quote ids for the whole tier-table universe from the same public master (exact match, EQ-series preferred, ambiguous → `unresolved` and unquotable), weekly-guarded heavy fetch ON THE MAC → `data/darling_ids.json`, shipped nightly by the eod_chain. |
| `src/ingestion/bhavcopy_clerk.py` | Daily EOD equity bars clerk (Dept 1, 2026-07-19, MAC-ONLY — the Dynamic Execution Layer's fuel line + the long-deferred bhavcopy daana): NSE full-bhavcopy `sec_bhavdata_full_DDMMYYYY.csv` (every equity's OHLCV **+ delivery qty/%** — the smart-money column) → raw day files `data/lake/bhavcopy/YYYY-MM-DD.csv` (capture-everything; drop-folder friendly — owner-supplied CSVs with the same naming just work, the flows_backfill precedent). NULL-honest (' -' → None never 0, EQ/BE series only); holidays are honest BC-404 no_file, nothing interpolated EVER; BC-4xx/5xx → `logs/bhavcopy_clerk.jsonl`; idempotent; 3-6s throttle. `bars_for(symbol, days)` reads chronological bars back for the pricer. First live backfill 2026-07-19: 15/15 weekdays captured (3 weeks). CLI `python3 -m src.ingestion.bhavcopy_clerk [--backfill N] [--date YYYY-MM-DD]`. |
| `src/ingestion/fo_bhavcopy.py` | F&O daily-bundle intake (Dept 1, 2026-07-20, DROP-FOLDER FIRST — the flows_backfill precedent: owner downloads NSE's Reports-Daily-Multiple.zip, clerk ingests): per trading day keeps fo.csv (F&O bhavcopy — INSTRUMENT×SYMBOL×EXPIRY with OPEN_INT/traded value), secban.csv (exchange MWPL ban list), fovolt.csv (per-underlying volatility) under `data/lake/fo_bhavcopy/YYYY-MM-DD/`. `liquidity_snapshot()` → `data/fo_liquidity.json`: per-underlying stock-options OI + traded value, documented TIERS (tier1 = top 25 by options traded value, not banned — the ONLY option-tradeable tier). Newest day WITH a bhavcopy wins (pre-open days carry only secban — the 20-Jul lesson). AUTOMATED same day (owner order: no manual downloads): `fetch_day/fetch_recent` pull the three files from NSE's static archives (verified URLs: archives/fo/mkt, fo/sec_ban, nsccl/volt; idempotent, html-error → honest miss, 2-4s throttle) and the patience_basket EOD chain now runs the F&O leg itself (`--fetch N` CLI too; live: 4 trading days auto-fetched). First real ingest 2026-07-20: 210 F&O stocks ranked, KAYNES banned (a caution-flagged darling — exchange validated our flag). NULL-honest, nested-zip tolerant, one bad member never kills intake. CLI `--ingest <zip>` / `--snapshot`. |
| `src/ingestion/integrated_results.py` | FRESH quarterly results clerk (Dept 1, 2026-07-20, MAC-ONLY — **the Issue-21 fix**): SEBI's integrated-filing regime (2025-) is where post-Jan-2025 filings actually live — `/api/integrated-filing-results` listing → each row's filed XBRL XML from nsearchives → stdlib parse of in-capmkt tags (RevenueFromOperations, ProfitLossForPeriod, BasicEPS, Face/PaidUpValue), rupees→LAKHS to match the financial_results capture schema exactly (screener + valuation consume unchanged). Consolidated preferred, revisions supersede, context-anchored parse (validated live: TCS Q1 FY27 rev ₹72,275 cr / PAT ₹13,420 cr / EPS 36.9 / 362 cr shares — 10-day-old filing). Same doctrine: throttled, IR-4xx/5xx → `logs/integrated_results.jsonl`, fail-open, NULL-honest. Overwrites the stale results-comparision captures. CLI `python3 -m src.ingestion.integrated_results [--queue\|--tickers ...]`. |
| `src/ingestion/nse_results.py` | Exchange-filed financial results clerk (Dept 1, 2026-07-19, MAC-ONLY — the Darling Pipeline's grain supply): NSE results-comparison API (the site's own Financial Results tab JSON) → `data/lake/financial_results/<SYM>.json` — last ~5 filed periods, net sales/total income/PBT/net profit/EPS/D-E + bank fields (NPA, capital adequacy; `is_bank` via interest-earned), amounts in Rs LAKHS as filed, NULL-honest ('-'/null → None never 0). House safe-crawl (reuses report_downloader's certifi session, 8-15s jittered throttle, one retry → NR-401/404/408/500 to `logs/nse_results.jsonl`, per-symbol fail-open). `--from-lake` sweeps every forensic-lake ticker. First live run 2026-07-19: 10/10 captured. CLI `python3 -m src.ingestion.nse_results --tickers ...`. |
| `src/ingestion/report_downloader.py` | Annual-report fetcher (Dept 1 clerk, 2026-07-19, MAC-ONLY — never the VM): fills `data/fundamental_reports/<TICKER>/` with an annual-report PDF from NSE's corporate-filings archive, for Dept 8's `annual_report_analyzer` to read. Reads `data/darlings_queue.json` (fundamental_screener's output; legacy `screening_queue.json` accepted as fallback) (the future Step-1 screener's output; `--tickers` overrides). Safe-crawl doctrine per `deals_tracker`: certifi SSL, cookie warm-up (best-effort — NSE 403s the homepage for scripted sessions but the API still answers; verified 2026-07-18), jittered 8–15s throttle between tickers (UNCHANGED — never tune this), ONE polite retry then an honest outage code (RD-401/404/408/500 → `logs/report_downloader.jsonl`), idempotent by filename, per-ticker fail-open (loop never crashes). Rejects non-PDF bodies. Defaults to the listing's newest `toYr`; `--fiscal YYYY` (`latest_report(listing, fiscal=...)`) picks that exact year's row instead for Dept 8's back-year fetches — no match is an honest RD-404, never a silent fallback to newest. Every network seam injectable (offline tests). First real run 2026-07-18: TCS/INFY/HDFCBANK/ITC/SBIN FY26, 5/5 downloaded. CLI `python3 -m src.ingestion.report_downloader [--tickers ...] [--limit N] [--fiscal YYYY] [--dry-run]`. |
| `src/ingestion/fundamental_parser.py` | Fundamental-statements ingestion (data-lake "capture everything" — NOT wired into the engine/Compass, pure data-gathering). Per tracked equity pulls the FULL annual + quarterly income statement, balance sheet, cash flow, and the whole 172-field `info` block from yfinance (Yahoo); nothing filtered. Isolated like `news_processor` (no core-trading imports, no trade state); yfinance is the only network dep, every seam injectable (`ticker_factory`) so it's offline-testable. NaN/NumPy/Timestamp values JSON-cleaned; missing fields stay absent (never a guessed 0); `derived` block computes NULL-honest revenue/net-income YoY+QoQ. One self-contained JSON per capture at `data/lake/fundamentals/<TICKER>/<capture_date>.json` (statements→period→line-item). Fail-open per ticker. DEP NOTE: needs `yfinance`+`pandas` (installed locally, NOT yet in requirements.txt). CLI `python3 -m src.ingestion.fundamental_parser [TICKER ...]`. |
| `src/ingestion/macro_lake.py` | The cross-asset macro lake, FRED leg (Dept 1 clerk, 2026-07-23, Macro Regime Engine M1 — multi-agent build, PM-reconciled): four decades-deep EOD series (BRENT/DXY/USDINR/US10Y) via FRED's official REST API (`api_key` from env/.env `FRED_API_KEY`; the public fredgraph.csv scrape endpoint HTTP/2-resets scripted clients, verified 07-23). Append-only idempotent `data/lake/macro/<KEY>.csv` (max-date rule so unsorted drop-folder files can't rewrite; atomic tmp+rename), FRED's '.' null stored as a VISIBLE empty-value hole (never 0.0, round-trips None via `read_series`/`parse_lake_csv`), fail-open per series with NAMED reasons (ML-401 no key — instant four-row fast-path on the real network path only / ML-404 shapeless / ML-408 timeout / ML-500), 2-4s jittered throttle, dry-run writes nothing. No key = named failure, never a crash; "no key" fails identically in tests and on cron (no injected-fetch exemption). Outages → `logs/macro_lake.jsonl`. Read-only on trade state. CLI `python3 -m src.ingestion.macro_lake [--series KEY] [--dry-run]`. |
| `src/ingestion/indices_lake.py` | The NSE indices leg of the macro lake (Dept 1 clerk, 2026-07-23, Macro Regime Engine M1-2): India VIX + NIFTY + 13 sector indices (incl. NIFTY Healthcare, added 07-23) from NSE's daily all-indices archive (`ind_close_all_DDMMYYYY.csv`, one static file per day, format+2019-depth verified live). Writes the SAME `data/lake/macro/<KEY>.csv` store as the FRED leg — INDIAVIX/NIFTY land exactly where `macro_features.SERIES` looks; sectors land beside for M3 playbooks. REUSES macro_lake's laws (append-only max-date, atomic write, NULL-honest `-`→None) rather than re-implementing; header located BY NAME so NSE column shuffles can't mis-parse; unmapped indices ignored; absent-from-file series NAMED per day. **Backfill walks FORWARD** (append-only would refuse a backward walk), weekends skipped, holidays honest IL-404 no_file, per-day fail-open, 3-6s throttle, ONE NSE job at a time (dev_workflow §3). Outages → `logs/indices_lake.jsonl`. CLI `python3 -m src.ingestion.indices_lake [--backfill N] [--date YYYY-MM-DD] [--dry-run]`. |
| `src/ingestion/index_history.py` | NSE historical-index CSV drop-folder clerk (Dept 1, 2026-07-23): ingests owner-downloaded per-year exports from NSE's Historical Index Data tool (the ONLY source of pre-2019 index history — it bot-blocks EVERY scripted/browser path we have: curl 503s on TLS-fingerprint, the in-app browser + real Chrome are both policy-blocked, so a human download is the only door). Parses the export (`Date ,Open ,High ,Low ,Close ,…`, DD-MON-YYYY uppercase, close col 4, '-'→None), reads the INDEX from the filename (`NIFTY 50-DD-MM-YYYY-to-…`, tolerates browser ` (N)` dup suffix) mapped to the lake KEY via the `indices_lake` roster (unmapped = NAMED skip). **MERGE mode, not append-only** (indices_lake only grows forward; this extends the lake BACKWARD): union by date, stored value wins on overlap (never rewrites forward data), atomic sorted rewrite, per-file fail-open, idempotent. First real backfill 2026-07-23: NIFTY 1995-11-06→2026 (7,550 rows, full inception history) — lit up the NIFTY benchmark channel for ALL 20 episodes (was 2021+ only). Sector-index + India VIX pre-2019 history still pending the owner's sector CSVs (M3 sector playbooks stay 2019+ until then). Outages→`logs/index_history.jsonl`. CLI `python3 -m src.ingestion.index_history --folder ~/Downloads [--dry-run]`. |
| `src/ingestion/intraday_tracker.py` | 15-minute intraday price snapshotter (READ-ONLY capture, NOT wired to trading): every 15 min during market hours fetches each `config/watchlist.yaml` ticker's live price and appends one JSON line to `data/lake/intraday_15m.jsonl` — the substrate for a future intraday-feature layer. Market-hours self-gated (`market_loop.is_market_open`), fail-open per ticker, fully injectable (`price_fn`/`clock`/`tickers`/`out_path`) for offline tests. Summaries NAME the failures (`failed_tickers`, capped at `MAX_NAMED_FAILURES`=12) as well as counting them — 2026-07-20: the VM logged a bare `failed: 2` every 15 min for days and nobody could tell which two; 2026-07-22 (Thursday-Protocol triage, owner started build early): one spaced in-sweep retry pass over the first-pass failures (`RETRY_SLEEP_SECONDS`=2, injectable `sleep_fn`) — the 15-26-name failure bursts were rate-limit collisions by SLOT, not dead scrips; both-pass failures stay named, summary gains `recovered`; imports only the data-only `dhan_client` + shared IST clock (no journal/portfolio/brain_map writes). NEEDS a valid Dhan token → live home is the VM (Mac token expires). CLI `python3 -m src.ingestion.intraday_tracker`. |
| `src/ingestion/corporate_events.py` | NSE corporate-announcements vacuum (2026-07-16). Fetches `/api/corporate-announcements` (cookie handshake + own Referer, like deals/flows), keyword-classifies each into CATALYST (order win/tender/contract), EXPANSION (merger/acquisition), STRUCTURAL_RISK (demerger/scheme), LEGAL_RISK (SEBI/subpoena/fraud/NCLT/default). Stores `data/lake/events/` per announcement day; fail-open, injectable `fetch_fn`, NULL-honest. NSE serves history DEEP (2,831 rows for a 2-wk 2020 window). Advisory-only, NOT wired into the engine (Phase-3 event gating consumes this). CLI `python3 -m src.ingestion.corporate_events [--from dd-mm-yyyy --to dd-mm-yyyy]`. |
| `src/ingestion/flows_backfill.py` | Historical FII/DII backfill INGESTOR (companion to `flows_tracker`). NSE's `fiidiiTradeReact` serves only the current day and moneycontrol is JS-rendered (verified 2026-07-16: no free machine-readable multi-year daily source), so history must be INGESTED from an owner-supplied export (StockEdge/Trendlyne/broker/Kaggle CSV or JSON) rather than crawled. `backfill(path)` normalizes flexible columns (case/space-insensitive aliases, multi-format dates, net derived from buy−sell, NULL-honest skip of unparseable rows) into the EXACT `flows_tracker` shape and lands one idempotent `lake.write_partition("flows", day, …)` per day; `load_flow_history()` is THE reader the Regime-Compass flow gate consumes ({date: {fii_net, dii_net, …}}). The live daily cron accumulates the series forward. CLI `python3 -m src.ingestion.flows_backfill --file history.csv [--dry-run]`. |
| `src/ingestion/flows_tracker.py` | Phase 1: NSE FII/DII daily cash provisional figures → `data/fii_dii_flows.json` + lake `flows/` + raw archive. Loose category matching (FII/FPI spellings drift), derives net when absent, never guesses zeros. Live → snapshot → "none". |
| `src/ingestion/earnings_calendar.py` | Phase 1: NSE results-calendar → `data/earnings_calendar.json` ({TICKER: next results date}, earliest-upcoming wins) + lake history; whole-calendar overwrite per run so postponements self-heal. `days_to_results(ticker)` is the deterministic, entry-filter-orthogonal feature (#50 lesson) consumers stamp at proposal time. |
| `src/confluence/evidence.py` | Phase 2 (holy-grail plan §5.1): the Evidence Snapshot substrate — one canonical per-layer record {layer, direction, strength, stance, detail, abstained} with adapters for technical/news/macro/affinity/flows/VIX + days_to_results; `build_evidence_snapshot` captures what every layer said at proposal time (explicit abstention, never a guessed neutral — #50's NULL-honesty), `summarize` renders the card lines, `persist_snapshot` stores to the additive `evidence_snapshots` table keyed by journal_ref (first capture wins). Capture-only: nothing scores or gates. WIRED (P2-1b): `capture_for_entry` stamps every headless proposal in options_proposer.run_headless (fail-open — a stamp failure never blocks a proposal); `persist_entry_snapshot` joins the snapshot to its outcome at plan_tracker.record_post_mortem, keyed by journal_ref. |
| `src/human_pulse.py` | Phase 3 (§6.6): the engagement tripwire for full paper autonomy — no human decide_pending action for N trading days (config `auto_approve_unsupervised_days`, default 3) while PAPER_AUTO_APPROVE is ON ⇒ NEW auto-approvals pause (entries stay pending, tracked hypothetically per #31) + one 🛑 unsupervised card per episode; ANY human decision re-arms instantly. State: data/human_pulse.json (atomic, hand-inspectable); fail-open toward autonomy on corrupt state; test-muzzled default writes. |
| `src/explain.py` | Phase 2 (§5.3): `python3 -m src.explain <short_id>` reconstructs a journaled trade end to end — entry + plan/spread economics, the frozen decision receipt (vix/analysis/vol-overrides/book/advisor lines), the six-layer evidence summary, outcome, and the brain_map evidence_snapshots join. No args lists recent entries. Read-only; honest absences for pre-substrate entries. |
| `src/graph_viz.py` | Knowledge-graph visualizer: `python3 -m src.graph_viz` reads brain_map.db (graph_edges + entity_affinity) and writes a fully self-contained interactive HTML (data/graph_viz.html) — Canvas force layout, provenance-colored edges (steel=outcome-derived causal, gold=smart-money affinity, red core=loss-permanent λ=0, ghosted dashes=expired), node size=degree, drag/zoom/search/inspect, dark+light themes, zero network/CDN. Read-only viewer. |
| `src/daily_context.py` | Phase 2 (§5.2): the daily Market Frame — one NULL-honest row per trading day in brain_map.db joining vix/band, macro index biases, news net sentiment, deals census, affinity biases, FII/DII nets (`build_frame` pure; `record_frame` upsert; `fold_lake` backfills every captured lake day; `run_for_today` = Sleep-Phase Task G, no-LLM, VM-safe). The cross-layer join surface the miners will read. Event-explosion deferred until its link semantics are documented. |
| `src/validation/stat_gates.py` | Phase 4 (§7.1): THE shared anti-hallucination toolkit — Wilson lower bounds (every displayed win-rate's honest number), exact binomial p, structural breakeven nulls (never derived from a pattern's own R — circularity fix), Benjamini-Hochberg over full mining batches, circular-block permutation nulls, split-window stability + concentration veto, support floors (15 itemset / 8 sequence / 10 promotion / 30 validation / 8 affinity episodes), the real-vs-sim policy (`promotable`: sim supports, never solely justifies), and the sim:/shadow:/trial:/placebo:/auto: self-poisoning exclusions. No miner defines thresholds inline. Noise-injection property test: ≤3 promotions per 1,000 pure-noise candidates. |
| `src/validation/placebo.py` | Phase 4 (§7.7): seeded information-free hypotheses that must NEVER validate — flow through the WHOLE harness indistinguishably (registered as ordinary candidates; harness is blind), run in a PARALLEL corrected batch (never inflating the real BH denominator), and their realized pass-rate is the measured false-discovery meter. `realized_fdr` compares the Wilson LB of the placebo pass-rate to the designed q (compare-to-budget, never void-the-batch; honest insufficient-n state); `audit_batch` is the ONLY code that reads placebo-ness. |
| `src/validation/digest.py` | Phase 4 (§7.8): the weekly Discord harness digest (the owner's chosen window) — tracked counts by lifecycle state, patterns validated/killed this week (Wilson-CI'd), the live-set degradation staircase, the governed-drought line, and the placebo false-discovery rate with its alarm. Plain-English-led, read-only over brain_map.db, Saturday 10:00 IST cron. |
| `src/validation/registry.py` | Phase 4 (§7.2): the pattern registry & lifecycle — every mined hypothesis registered with a FROZEN canonical-JSON predicate (pattern_id = its hash; re-discovery is a no-op, dead ends stay dead per #49), governed states CANDIDATE→TRIAL→VALIDATED→LIVE_ADVISORY / QUARANTINED / INSUFFICIENT_N (distinct from failure) / DEAD (terminal; second quarantine forces it), every transition audited, soft-only (rows never deleted, #37). `citable()` gates what NEW discovery surfaces may cite; validated patterns mint `auto:` tags that stat_gates excludes from miners; `states_for_tags` (fail-open, never load-bearing) maps a proposal/forecast's active tags -> registry status so existing #26/#33 surfaces render the inline `[registry: ...]` stamp (wired into forecast._memory_lookup) without gating anything. Additive tables in brain_map.db. |
| `src/validation/noise.py` | Phase 4 (§7.1): the end-to-end false-discovery regression — feeds the FULL real pipeline (cooccurrence+sequence miners → registry → trial → promote) deterministic pure-noise histories (labels Bernoulli at the structural breakeven null, independent of every tag) in throwaway temp DBs and asserts total promotions stay under the binomial upper bound at the ACTIVE fdr_q; forced candidates keep the trial gate exercised even when BH correctly kills everything; `plant_edge()` is the positive control (a genuinely predictive tag-pair MUST be mined and promoted — sees real edges, ignores fake ones). Hard guard refuses any conn resolving to the production brain_map.db. **v2 `--mode bars` (the PRICE path):** a fixed base series block-permuted per seed (return marginals preserved exactly, date-aligned structure destroyed) runs through the REAL `run_simulation` — window A's organic sim trades are mined (corpus="sim"), candidates judged on window B's ORGANIC matched evidence against their FAMILY's own window-B base rate (never breakeven/50% — the sim corpus's structural ~62-79% generosity must be absorbed by the family bar, not credited as edge; pinned by test). CI smoke = 8 label seeds + 3 bars seeds; nightly `python3 -m src.validation.noise --seeds 500` / `--mode bars --seeds 50` (exit 1 on breach). First runs: labels 8/172 (4.7%) vs bound 37; bars 0/20 vs bound 7, 333 organic resolutions — both at q=0.15. |
| `src/validation/trial.py` | Phase 4 (§7.3-7.4): walk-forward trials + shadow tracking — `split_windows` (discovery/validation split with a 5-day embargo so cross-boundary resolutions can't leak), `record_shadow_fire`/`resolve_shadow` (prospective `shadow:` firings into brain_map's `shadow_trades` table — NEVER journal.jsonl, so a shadow can't arm the cooldown or block a real proposal), `shadow_evidence` (validation-window-only, market-date keyed), `evaluate_trial` (promotes via stat_gates.promotable — real+sim strata, Wilson LB beats the structural null, real evidence required; drives the registry to VALIDATED / INSUFFICIENT_N and records oos_stats), `sim_evidence_in_window`/`generate_window_b_evidence` (a trial can SELF-PRODUCE its out-of-discovery sim stratum by running the real simulator over [validation_start, end] with the §5.5 T+1-open params forwarded verbatim — sim still supports, never solely justifies), `learning_corpus_filter` (self-poisoning guard). Runtime-spy tested to never touch journal/portfolio. |
| `src/validation/monitor.py` | Phase 4 (§7.6): validation-is-a-lease drift monitor — `cusum_breach` (one-sided CUSUM on live shadow win/loss vs the validated rate) + a Wilson-crossing tripwire auto-QUARANTINE a bleeding pattern (owner: auto-quarantine + notify, one Discord card per event; second quarantine = DEAD via the registry lineage rule); adaptive lease expiry (max 90d / K live matches) demotes a stale pattern to CANDIDATE for re-trial on a grown sample. `run_sweep` = Sleep-Phase Task H (no-LLM, VM-safe). No journal/portfolio writes. |
| `src/discovery/cooccurrence_miner.py` | Phase 5 (§8.1): the first miner — pure-Python Apriori over resolved-outcome transactions (each outcome's minable event tags UNION its market-day `ctx:` tags from daily_context). REAL and SIMULATED corpora mined SEPARATELY (the is_learnable_ref split), win-rates tested against a STRATIFIED base rate (underlying × vix-band, thin strata fall back to global) so it can't rediscover the pipeline's own gates, exact-binomial + Benjamini-Hochberg across the whole batch, directional (a significantly-WORSE cluster is not a BUY candidate). Survivors register as CANDIDATE patterns via the registry (frozen-definition idempotent); it ENUMERATES, never surfaces. Deliberately NOT wired into the nightly sleep phase yet (panel: defer until enough live daily_context rows accrue); near-zero survivors on thin data is the correct output. |
| `src/cycles.py` | Owner concern #4 / H3 (Way A, 2026-07-11): the calendar-cycle vocabulary as PURE date math — era-aware monthly expiry (last THURSDAY pre-2025-09-01, last TUESDAY after the NSE migration), expiry-week/expiry-day/quarter-end/month tags (`cycle_tags`), per-ticker results-proximity `event_tags` (live-forward only — the earnings calendar can't be as-of recomputed). Two consumers earn meaning from it: the miners (`ctx:season:*` daily_context tags, auto-backfilling since pure) and the forecast's Way-A cycle driver. Nothing hand-adds points for a cycle (composition law #63). |
| `src/discovery/shadow_runner.py` | Owner concerns #1/#2 (2026-07-11): live shadow-firing of registered patterns. At every REAL proposal entry (hooked into the shared evidence stamp, test-muzzled), the entry's full tag picture (own tags ∪ today's ctx ∪ lagged antecedents) is matched against every fireable pattern (CANDIDATE/TRIAL/INSUFFICIENT_N + VALIDATED/LIVE_ADVISORY for the drift stream; DEAD/QUARANTINED never fire); each match records a host-linked `shadow_trades` row. Sleep-Phase Task I (`resolve_from_outcomes`) copies the host trade's resolved outcome onto its shadows — the patterns' out-of-sample evidence stream, one resolver, no parallel price math. Runtime-spy tested to NEVER touch journal/portfolio: a shadow fire is bookkeeping, the live pipeline is byte-identical with or without it. |
| `src/discovery/inspect.py` | Owner concern #2 (observability): `python3 -m src.discovery.inspect [needle]` reconstructs any discovered pattern in plain English — frozen definition, mining provenance, in-sample stats (labeled "DISCOVERY data, counts for nothing"), out-of-sample reality, full lifecycle audit trail, and every shadow fire with host + result. Read-only; skepticism exercised on facts instead of trust. |
| `src/discovery/strategy_evidence.py` | Phase 5 (§8.6, the read-only substrate — NOT the champion/challenger duel, which stays human-gated per #49): the "check WHICH strategy" evidence view. For a pattern (tag set) it breaks resolved outcomes down BY STRUCTURE (archetype), keeps REAL and SIMULATED strata apart (sim can never earn a preference), refuses to render a structure below `MIN_VIEW_RESOLUTIONS` real resolutions (5, config-tunable), carries every win-rate's one-sided Wilson lower bound, and decides `preferred_structure` on the LOWER bound not the headline — PREFER only when the top structure's LB beats the runner-up's headline rate (genuine separation), else ABSTAIN (overlap / nothing clears the floor). `summarize` emits ONE PREFER/ABSTAIN verdict (composition law #63). Read-only; recommends nothing not earned. |
| `src/discovery/run_miners.py` | Phase 5 (§8.3): the discovery-pass orchestrator — runs every miner (cooccurrence + sequence) over both corpora (real + sim) in one call and returns an HONEST combined report (per-corpus transaction counts, survivors, newly-registered), fail-open (one miner throwing is logged as an error entry, the pass continues). Leads with the plain-English headline that frames a thin-data zero as CORRECT and shows the support reality (transaction counts) that explains it. Scheduled via the GATED wrapper `discovery/nightly.py` (decision #76) — never call this directly from cron. |
| `src/discovery/nightly.py` | Decision #76: THE cron entry for the Phase-5 miners (VM, daily 20:20 IST — after sleep_phase's drift monitor, before the 20:30 ops sweep so its heartbeat is checkable). Triple gate before `run_miners.run_all`: (1) `ops_monitor.check_heartbeats()` — any silent scheduled job today skips (real-time); (2) the LATEST ops sweep's `problems.jsonl` slice restricted to the INGESTION logs (the miners' upstream) must be clean — an error line from today's evening jobs gates tomorrow, not tonight (documented one-night trade; absent readings are NULL-honest); (3) `daily_context` >= 60 frames (the old manual-only panel rule as a number). Skip = exit 0 + one log line; every 7th consecutive skip = one Discord note (state ledger `logs/.discovery_nightly_state.json`) — the gate can never die silently. Health read IN-PROCESS from ops_monitor's own signals (same sources as the dashboard health map), never over HTTP. Heartbeat-monitored (`discovery_nightly.log` in EXPECTED_JOBS; touched even on skips). Imports nothing from news_parser/text_intelligence. Manual: `python3 -m src.discovery.nightly`. |
| `src/discovery/sequence_miner.py` | Phase 5 (§8.2): the lagged "A-then-B" miner — the co-occurrence core lifted onto a time axis to test the owner's H1/H2 theses (an early tell that PRECEDES the move). Each resolved outcome's transaction carries `lag{k}:<ctx-tag>` items = the daily_context frame k trading-days BEFORE the trade's ENTRY (anchored on the min linked-event date, never exit). NO look-ahead by construction — the lag-k frame is k positions earlier than the as-of frame (most recent dated ≤ entry), always strictly before entry; proved by a timelock `assert_future_blind` test (a discovery feature doesn't merge without one). Reuses cooccurrence_miner.mine verbatim (stratified null, support floor, exact-binomial, batch BH, directional), real/sim mined separately, survivors register as `sequence` CANDIDATEs. Not wired into sleep phase yet; short series → zero survivors is correct. |
| `src/validation/timelock.py` | Phase 2 (§5.4): the as-of contract, mechanically checkable — `assert_future_blind(compute, base, salted)` proves a discovery-facing computation's output is invariant under future-dated input perturbation (`semantic_equal` ignores formatting, never drift; `future_salt` builds the perturbation). Enforced by tests/test_no_lookahead.py in the decision-#30 import-guard style; new discovery features don't merge without one. |
| `src/knowledge_graph/entity_affinity.py` | Phase 8 (decision #61): accumulates `data/deals_history.jsonl` into an entity↔promoter-group affinity graph — `canonicalize_client`, `load_entity_groups`, `accumulate_entity_affinity` (folds per-day, idempotent; projects a decaying `concentrates_in` edge into `graph_edges` for linked pairs only), `build_affinity_readmodel` → `data/entity_affinity.json`, `evaluate_distribution_signals` → DISTRIBUTION/ACCUMULATION advisories in `logs/affinity_advisories.jsonl`. All-time concentration + recent-window net direction, both FUTURE-BLIND (decisions #63/#64): readmodel answers about day T derive purely from history rows dated <= T (`_concentrations_as_of`, horizon-bounded `_recent_flows`); the accumulation table serves the write path only. No-LLM, VM-safe; runs as Sleep-Phase Task F. **Specialist-vehicle scan (owner thesis 2026-07-17)** in the same pass: `find_specialist_entities` flags clients whose ENTIRE disclosed footprint is <=2 tickers with >= ₹5 cr gross — no group map, no %-lens; status by window shape (`active_burst` "ekdum aayi hai" / `vanished` "fir gayab" / `persistent_specialist`) → `data/specialist_entities.json` (censoring caveat in the header) + real-time Discord card for NEW specialists (de-dup ledger `logs/specialist_alerts.jsonl`, fail-open, failed send doesn't mark seen). The group-concentration link (#61 thresholds) is UNCHANGED — the specialist lens runs beside it, not instead. Advisory only — no trades, not wired into scoring. |
| `src/knowledge_graph/resonance.py` | Phase 7: `evaluate_portfolio_resonance(parsed_event, macro_matrix)` cross-refs incoming flow against open journal positions → CONFLICT (exit/cut-loss advisory + expiry-roll when the LONG thesis survives) / RESONANCE (extend-target + concrete strike-roll from the position's own legs) / NEUTRAL, horizons blended by days-to-expiry. Journal via pure file stream; brain_map strictly `mode=ro` (one conn per sweep, memoized per strategy); advisory-only, writes nothing except `log_advisories` → `logs/resonance_advisories.jsonl`. |
| `src/decay_engine.py` | Phase 6E temporal decay sweep: `migrate_schema` (adds `valid_from`/`invalid_at`/`decay_lambda` to `graph_edges`), `apply_decay_sweep` (daily `w(t) = w₀·exp(−λ·t)` pass — reduces `confidence_score`, stamps `invalid_at` when weight < 0.1). Run via `python3 -m src.decay_engine` or after the Sleep Phase cron. |
| `src/vol_bridge.py` | Phase 6F quantitative execution bridge: `classify_regime(edges)` classifies "Expansion"/"Contraction"/"Neutral" from aggregate signed edge weights; `compute_regime_overrides(conn, mode)` queries the live graph and returns `build_proposal` kwargs — under Expansion either `risk_pct` (×0.70, scale_risk mode) or `short_strike_otm_pct` (×1.50, widen_wings mode). Stateless, no writes, fail-safe. Wired into `market_loop.fetch_market_state` + `options_proposer.run_headless`. |

## Signal & suggestion engine

| File | Purpose |
|---|---|
| `src/rules.py` | Alert rule evaluation: `price_above`, `price_below`, `percent_up`, `percent_down`. |
| `src/suggestions.py` | Trend (50/200 SMA) + momentum (14-day RSI) read per stock; `analyze()`/`describe()`. |
| `src/suggest.py` | Entry point: emails a daily suggestions digest (`python -m src.suggest`). |
| `src/news_processor.py` | Isolated: Google News RSS -> Gemini -> `data/news_sentiment.json`. v3 (2026-07-20): DUAL-HORIZON — `short_term_catalyst_score` (days; `sentiment_score` stays == it for back-compat) + `long_term_macro_score` (months; None = unknown, never 0), each read `prev`-linked to the prior fresh run with `reversal` flags per horizon (crossed neutral + moved ≥3 pts) → one 📰 Discord note per run when flagged. Owns freshness for every consumer: `entry_is_fresh()` (48h; Issue 22 — the `stale` flag never ages, so age is judged at read time). Imports NO core trading code (isolation principle; the notify is a lazy fail-open side-door). Consumer map: DATA_CONTRACT.md §2.3. |
| `src/forecast.py` | Combines technicals + news sentiment into a transparent, rule-based checklist: bias/confidence/drivers/time-horizon. Reads `data/brain_weights.json` for learned per-archetype adjustments. |

## Trading & risk

| File | Purpose |
|---|---|
| `src/strategy.py` | Turns signals into full trade PLANS: entry rule, stop-loss, target, risk:reward, rationale, risk-based position sizing. `propose_plans()` / `propose()`. |
| `src/portfolio.py` | Paper portfolio math: `data/portfolio.json`, buy/sell, cash + 25%-per-stock rails; the SPAN margin model (`calculate_span_margin`, hedge-offset) + friction math; `span_stress_factor(vix)` + `VIX_STRESS_BANDS` (2026-07-19: 1.0 calm / 1.15 at VIX≥16 / 1.30 at VIX≥25, a deliberately simple paper model of exchange stress margin hikes — recalibrate against NSE circulars; fails toward 1.0 on bad input). |
| `src/margin_audit.py` | Dept 3 SPAN margin audit (2026-07-19, READ-ONLY report CLI `python3 -m src.margin_audit [--json]`): replays every journaled spread — DRIFT check (recorded margin vs today's `calculate_span_margin` from the recorded legs), entry-time stress at the entry's own `receipt.vix` (no VIX = honestly unstressable), and the PANIC scenario (open-book reserve at ×1.30 vs the Rs.10L pool + greedy replay counting entries that would no longer fit). First real run 2026-07-19: 14 spreads, 0 drift, panic adds Rs.42,143 to the open book (18.3% of pool total), 0 squeezed out at current book size. Injectable entries/pool; writes nothing. |
| `src/adaptive_sizing.py` | Decision #81 (owner Directive 2): the autopsy-driven sizing feedback loop — pre-sizing consult over the system's OWN resolved record (equity: shadow-ledger autopsies paired by id, gap-shock losses half-weight; options: journal outcomes with performance.py's #72 exclusions). Break-even-centered Beta prior (strength 10 — zero data = exactly 1.0x, no coin-flip learning #44/#50); p* empirical only with ≥3 weighted wins AND losses (else nominal 0.40/0.50); Wilson bounds from `stat_gates` (single source). Asymmetric paths: PENALTY ≥4n (floor 0.25x, fast), VETO ≥8n only when the Wilson UPPER bound < p* (telemetry row still logged), BOOST ≥10n only when the LOWER bound > p*+0.05 (cap 1.5x, inside existing desk caps). Ticker overlay veto-only (≥5n). Seams: `equity_verdict` (fund_entry risk-budget multiplier) + `adjust_option_lots` (floor 1 lot / veto pre-margin; no boost — size_lots is already the risk cap). Non-neutral → `logs/sizing_adjustments.jsonl`; ONE card per key STATE CHANGE (ledger = de-dup memory). Kill switch `adaptive_sizing_enabled`; every seam fails OPEN to neutral. CLI `python3 -m src.adaptive_sizing` = read-only report of every key's current verdict. |
| `src/firm_treasury.py` | **v2 — SINGLE-MACHINE (decision #83):** the router (unchanged #80 tilts: NIFTY/depth/value/VIX/options-demand; 15–60% clamp, ₹25k round, ₹50k deadband, ₹1L step) now moves ONE ROW — `treasury_state.equity_budget_rs` in brain_map.db, atomically (`get_budget`/`set_budget`). Two-phase/mirror-locks/reconcile/SSH-state DELETED (one cash pool behind pm.request_entry makes double-spend structurally impossible). Inputs all VM-local: live VIX + NIFTY trend (resident token), utilization from own non-`eqd:` locks, Buy-depth from the shipped tier table. Budget is SOFT (unspendable past firm liquid — the one door judges cash). VM cron #21 19:50 IST. `vm_push_file` = the Mac→VM ARTIFACT lane (tiers/levels/ids — pure data, no invariants). Ledger `logs/treasury_ledger.jsonl` + one card per rotation. Old #80 row for history: |
| (history) #80 | Decision #80 (owner Directive 1): nightly dynamic capital routing between the desks — mechanical regime router (base 30% equity share; tilts: NIFTY uptrend / Buy-tier depth / deep value / high VIX / options demand; clamp 15-60%, ₹25k rounding, ₹50k deadband, ₹1L max step; unknown inputs tilt zero). Moves capital via SUBSCRIBE/REDEEM on the Mac desk's `starting_capital` (peak shifts with base — dd% stays rupee-honest; redeem hard-clamped to liquid cash) + the VM's `equity_desk_allocation` lock, under the RAISE-FIRST two-phase invariant E_vm ≥ E_mac (partial failure = idle capital, never double-spend; reconcile E_vm := E_mac converges both failure modes, no intent file). Runs inside `patience_basket.eod_chain` after grading, before the shadow leg; VM inputs via one SSH round-trip (`--vm-report`: allocation, account, VIX, NIFTY trend, exhaustion count; `--set-equity-allocation` mutates the lock directly — accounting transfer, halt stack N/A). Unreachable VM = frozen split; 3rd consecutive night = one warning card. Ledger `logs/treasury_ledger.jsonl`; one card per rotation. Kill switch `treasury_enabled` (code default OFF). CLI: status / `--rotate [--dry-run]` / VM modes. |
| `src/equity_desk.py` | **v2 — VM-NATIVE (decision #83, supersedes the #79 separate-file desk):** the equity desk inside the ONE firm database. Desk books = VIEWS over `eqd:`-tagged `margin_locks` rows (`desk_state`: budget from `firm_treasury.get_budget` + realized + deployed); funding through the single `pm.request_entry` door (adaptive consult → sizing on desk capital, 1%/15% caps → per-desk ruin halt at −10% of budget → budget gate → firm cash+halts); settlement nets 2026 delivery frictions into firm equity via `pm.release_margin`. LIVE cycle `run_darling_live_cycle` (market-loop leg, master_scheduler root): exits at live quotes (proposer's own resolver), Strong-Sell force-exits, settlements, then live entries — quote INSIDE the strict buy zone, `fill_basis:"live"`; tier table freshness-gated (3d), quote ids from the Mac-built `data/darling_ids.json` (14d gate; `security_id_for`/`live_quote` — unmapped = unmarked, never guessed). `render_book_lines` = the live equity section on every card. Telemetry rows survive every funding failure/veto. CLI: book view / `--sweep`. Old #79 row follows for history: |
| (history) #79 | Decision #79 (owner ruling 2026-07-20): the darling shadow book's paper-capital desk — a slice of the firm's 10L (config `equity_desk_capital_rs`, default Rs.3,00,000) run through Dept 3's conn-generic `portfolio_manager` against the desk's OWN sqlite (`data/equity_desk.db`, Mac-side): same tables, same 10% risk-of-ruin halt, same daily 3% breaker, same silent margin-exhaustion — zero re-implemented risk rules. The matching VM-side reservation lock `equity_desk_allocation` (run ONCE, ON THE VM: `python3 -m src.equity_desk --reserve-firm-slice`, idempotent) keeps the firm total at 10L. Seams injected at patience_basket.eod_chain ONLY: `fund_entry` (risk-based whole-share sizing — 1% risk / 15% notional cap, the risk budget scaled by `adaptive_sizing.equity_verdict`'s multiplier per #81 (veto = funded ₹0 with `vetoed_by` reason, telemetry row kept) — then `request_entry` at the single door; fails CLOSED, telemetry never stops) and `settle_exit` (P&L net of the 2026 DELIVERY friction stack — STT both sides, buy stamp, exchange+SEBI+GST, DP debit — released via `release_margin`). `broadcast_activity` = ONE card per EOD run, only when money moved. `--sweep` reconciles orphan locks; bare CLI prints the desk summary. Kill switch `equity_desk_enabled` (code default OFF — a stale VM config can never fund). **ONE-FIRM-VIEW (#82):** `publish_snapshot()` (Mac EOD chain → `data/equity_desk_snapshot.json`: account + funded open positions marked at latest bhavcopy close; telemetry-only counted not listed) + `load_snapshot()` (VM reader — stale >30h flagged, missing = None) + `render_firm_lines()` (the equity section on the 2h card / EOD summary / CEO brief — labeled "EOD marks", never presented as live; unmarked rows "—"). Shipped via `firm_treasury.vm_push_file`. |
| `src/portfolio_manager.py` | Phase 6G capital & margin allocation layer: Rs.10,00,000 simulated account pool in `brain_map.db` (additive tables `account_state`/`margin_locks`/`equity_curve`/`account_events`). `request_entry` locks SPAN margin per entry (silent reject on margin exhaustion), `release_margin` settles realized P&L + ratchets peak equity, composed ENTRY-HALT LIST `ENTRY_HALT_CHECKS` (review #2 halt-stack rule, 2026-07-19): (1) hard 10% trailing-drawdown risk-of-ruin halt (`MAX_DRAWDOWN_PCT`), (2) DAILY 3% realized-loss circuit breaker (`MAX_DAILY_LOSS_PCT`, merged from `next_gen_engine/portfolio_risk_manager.py` — entries only, IST-day reset by construction, reads its own `margin_locks.released_at`/`pnl_net`, one de-duped Discord card per day via `fire_broadcast` when it rejects; pure `check_daily_breaker`/`realized_pnl_today` kept for the simulator). `required_margin_for` applies the ENTRY-TIME VIX-stress factor (`portfolio.span_stress_factor`, owner decision 2026-07-19 — panic reserves more upfront so margin exhaustion naturally chokes entry count; calm/unknown VIX = byte-identical base). All timestamps IST (Issue-16 discipline). Gates `run_headless` only when trading the real paper book (injected books bypass — decision #40); fail-open seams `gate_headless_entry`/`release_entry`. Inspect: `python3 -m src.portfolio_manager`. |
| `src/trade_planner.py` | Phase 6I technical-to-options planner: `map_technical_to_strategy(technical_state)` — pure evaluation matrix (trend × IV regime → iron_condor / bull_call_spread / bear_call_spread / bear_put_spread / no_trade) with structural leg specs (side, CE/PE, strike + offset from ATM, Bank Nifty grid). Support/resistance override the 2%-OTM shorts; never contradicts the VIX-16 regime gate (decision #42). Phase 6J: every tradeable plan carries theoretical economics (`estimate_plan_economics` — leg premiums from the simulator's synthetic-chain model, net credit/debit, max profit/loss per lot — never Rs.0 placeholders). Zero side effects. |
| `src/market_loop.py` | The entry-side daemon body (composed by master_scheduler): `fetch_market_state` (trend read + India VIX + vol-bridge overrides) → `options_proposer.run_headless` per index during 09:15–15:30 IST; per-index 2h `CooldownRegistry` (`seed_from_journal` rebuilds it across restarts from `created_at`); `is_market_open`/`ist_now` = the canonical IST clock everyone imports. `shadow_fn=` (2026-07-17): optional zero-arg Shadow-Equity telemetry cycle run once per open-market poll after the options pass, fail-open, default None=OFF — only the composition roots (master_scheduler `_shadow_cycle_fn`, `__main__`) wire it, so tests/simulator never touch it. |
| `src/equity_shadow_proposer.py` | The Shadow Equity Engine (owner directive 2026-07-17): paper-tracks the block-VWAP pullback on deal-covered equities (deals ledger ∩ SECURITY_ID_MAP, ~73 names) purely for telemetry — "log the false positives to train a better model". Entry = live price in [block-VWAP floor, floor+5%] (signal source: `smart_money_trend`); stop = floor−2% (thesis break = the smart-money floor failing), target 2R, time-stop 10 calendar days; 90d accumulation flag AND sector verdict are RECORDED not gating (requiring accumulation left ~0 qualifying names on the real ledger — failure analysis wants features, not filters). EVENTS use the owner's four-question learning frame: entry = `kyu_trigger` (WHY: signal line + block_vwap + accumulation + trigger deals) / `kaise_context` (HOW: VIX + sector verdict + NIFTY trend) / `kya_kara_action` (WHAT: entry/stop/target/simulated_risk_pct); exit = `kya_sikha_autopsy` (LEARNED: rule-based `categorize_failure` — Gap-down shock ⟩ sector-drag ⟩ VWAP-trap priority — + r_multiple/held_days/sector_at_exit/vix_at_exit). HARD CONTRACT: every event `mode="PAPER_TELEMETRY"` + `capital_allocated=0`; never imports journal/portfolio_manager/options_proposer/notifier/brain_map (test-enforced); one open shadow per ticker, no same-day re-entry; fail-open per ticker; every context source injectable (`sector_fn`/`nifty_trend_fn` — tests stay offline). `run_cycle()` = the market_loop seam (exits resolve before entries). **DARLING LEG (F&O tranche step 5, owner-approved 2026-07-20; re-wired to the 7-tier lifecycle system the same day): `run_darling_cycle()`** — resolve open shadows on bhavcopy closes (stop/target/time) → **force-exit Strong-Sell grades** (Directive 2 No-Orphan rule: pinned names exit as `fundamental_break`, valuation-extreme grades as `strong_sell_tier`; weak_sell never force-exits — the position's own stop is the thesis-break detector) → paper-buy every entry-eligible Buy-tier name (`entry_eligible_rows`: strong_buy + weak_buy that is IN the zone — near-zone weak_buy is never chased) at the day's bhavcopy close (`fill_basis:"eod_close"` honesty stamp; Mac EOD chain, no token, VIX recorded None) through the `equity_entry_checks` halt stack (single enforcement door — never re-implemented here); stop = the pricer's zone-failure stop; target = the first trim pivot paying ≥1R (a sub-1R pivot would mint instant fake wins — the LTF catch on first live run) else 2R; per-entry `time_stop_days=45` override (patience thesis ≠ the block leg's 10d); setup id `darling_buy` (legacy `darling_ripe` ledger rows still autopsy as darlings). Reads `data/darling_tiers.json`; same journal, same dedup, darling-specific autopsy texts. Wired at the `patience_basket.eod_chain` composition root, fail-open. First live run 2026-07-20 (pre-tier): 12 names logged, all R:R ≥1.02. **CAPITAL ERA (decision #79, 2026-07-20):** `run_darling_cycle(capital_fn=, settle_fn=)` — the injected equity desk sizes/locks funded entries (stamped `mode="PAPER_CAPITAL"` + notional; exits ride the entry's stamp) and settles freed capital BEFORE the day's entries; `capital_fn=None` (every legacy caller/test) stays byte-identical zero-capital telemetry, a funding rejection keeps the telemetry row with its reason, and the import ban (no Dept 3 in this module) still holds test-enforced. |
| `src/knowledge_graph_logger.py` | Append-only JSONL telemetry event store for the Shadow Equity Engine → `logs/equity_shadow_journal.jsonl` (gitignored): `log_event` (IST-stamped, fail-open — returns `_persisted` honestly instead of raising), `read_events` (junk-tolerant), `open_positions` (entries unpaired by exit id). Deliberately NOT a brain_map table: the Brain Map feeds the options engine's query_similar_events and zero-capital telemetry would skew it — a later explicit mode-tagged ingest is the path in. |
| `src/options_proposer.py` | Phase 5 spread proposer: trend read → `StrategyConstructor` legs → adaptive-sizing consult (#81: `adjust_option_lots` after `size_lots` — penalty floors at 1 lot, an EARNED veto refuses the proposal; fail-open, no boost since size_lots is already the risk-budget max) → exposure gate (`exposure_gate.gate_entry`, decision #68 — before margin so a blocked duplicate never locks it) → margin gate (`pm.gate_headless_entry`, skipped for injected sandbox books) → journal PENDING_APPROVAL + Discord alert. Honest paper fills (decision #70): `_leg_fill` prices BUY legs at the chain's `top_ask_price` and SELL legs at `top_bid_price` (per-leg `fill_basis: "quoted"` stamped on the spread legs), falling back to `last_price` (basis "ltp") when the quoted side is missing/zero or >50% off a live LTP — simulator/LTP-only chains stay byte-identical. `decide_pending(short_id, approve, why)` is THE decision seam (CLI --review-pending, API bridge, Discord buttons, auto-approve all converge here). `PAPER_AUTO_APPROVE` env switch (default OFF, decision #53; NEVER applies to injected books) auto-approves headless proposals through the same path. |
| `src/analyst.py` | Gemini post-mortem on every tracker-resolved trade (plan vs actual → variance/unexpected/guardrails JSON) stored in `outcomes.post_mortem` — fail-safe, resolution never blocks on the LLM. |
| `src/wealth_lock.py` | Paper-scope Wealth-Locking Flywheel: 50% GOLDBEES sweep on profitable settlements, hooked from `portfolio_manager.release_entry` (whose released=True answer is committed BEFORE the sweep and never flipped by a sweep failure). **FLYWHEEL MERGED 2026-07-20:** the earmark now becomes a SIZED PAPER ORDER — `next_gen_engine/wealth_flywheel.build_sweep_order` graduated into `size_sweep_order` (staging file deleted, the anti-orphan rule) — whole GOLDBEES units only, un-investable remainder reported as `cash_residual_rs`, never rounded in; ledger gained additive `order_qty`/`cash_residual_rs`/`sizing_blocked_reason` columns (pre-merge rows read NULL). Unblocked by `ingestion/scrip_master` verifying GOLDBEES = NSE_EQ id **14428**, now in `SECURITY_ID_MAP`. THE ID GATE (owner's Null-Honesty condition on the merge): `goldbees_verified()` re-reads the weekly clerk's report AT CALL TIME (the Issue-22 lesson — a `stale` flag that never ages is how an 11-day-old file got trusted) and sizing runs ONLY on a fresh explicit `ok`; mismatch / id_not_found / stale (>14d = two missed weekly runs) / `unavailable` / missing report all BLOCK sizing — the sweep is still recorded with the rupee amount, the reason stored on the row and stated on the card (⚠ sizing skipped), i.e. the degraded mode is exactly the pre-merge behavior. `default_price_fn` quotes through SafeDhanClient (the one door #48), fail-open to None, and is muzzled under pytest (PYTEST_CURRENT_TEST, the notifier's pattern) so a unit test never reaches the network. Still advisory: a LEDGER row + card, never a cash movement. |
| `src/positions.py` | Read-only open-positions view (terminal, gateway `GET /api/discord/positions`, bot `/positions`, and the exposure gate's read): predicates IMPORTED from `plan_tracker._trackable`/`_spread_trackable` — single source, the view and the tracker cannot disagree. Spread rows carry the stamped `direction` (additive; None on pre-stamp legacy lines). `src/view_positions.py` is its ASCII-table CLI (`python3 -m src.view_positions`). |
| `src/portfolio_report.py` | 2-hourly read-only Discord report card (cron `0 */2`, market-hours self-gate) AND `get_live_marks()` — THE shared mark ladder every consumer uses (dashboard + card): engine snapshot first (zero Dhan calls), direct SafeDhanClient fetch only for uncovered positions (equities; stale snapshot). `read_exposure` = mode=ro SELECTs on the capital tables. `_spread_detail` = the one wording for spread marks. |
| `src/portfolio_greeks.py` | Decision #71: portfolio-level Greeks advisory (the Stage-5 gap from `docs/gemini_research_gap_analysis.md` §5 — the engine thought per-trade; ten condors are one concentrated short-Vega bet). Aggregates the open paper book's net delta/theta/gamma/vega from Dhan's own per-strike chain Greeks (`leg_greeks` reads the nested `greeks.{delta,theta,gamma,vega}`, no Black-Scholes engine), signs BUY+/SELL− × lot×lots (`position_greeks`), and rolls up (`aggregate`) with each position priced on ITS OWN expiry's chain. Two equity-scaled binary budgets (#63, no scores): net VEGA (Rs. loss under a `greeks_vega_shock_points` IV shock vs `greeks_vega_budget_pct` of equity) and net DELTA notional (per-point delta × spot, additive across underlyings, vs `greeks_delta_budget_pct`); theta/gamma reported not budgeted (gamma/pin-risk already owned by the 2-day pre-expiry exit #27). ADVISORY-ONLY (#41): reads journal+chains, writes only its snapshot ledger `logs/greeks_snapshots.jsonl`; HONEST ABSTENTION — a leg with no chain Greeks makes its whole position un-priceable, excluded+counted never guessed (#50). One card per breach-type per IST day (ledger IS the memory, exposure-gate #68 pattern). NOT wired into the live loop — standalone advisory + CLI `python3 -m src.portfolio_greeks` (eod_summary/portfolio_report class); kill switch `portfolio_greeks_advisory`. |
| `src/book_context.py` | Decision #73: the "what we hold and WHY" read-model (owner ask 2026-07-15). `position_dossier` = one open spread's entry reason (`signal`), stamped direction, entry regime, age, expiry distance, capital at risk — all from what the journal recorded at birth, nothing fetched; `book_summary`/`render_book` = the whole book with reasons + shape (counts by direction/underlying, total max loss), journal+date-math only so it answers identically mid-session, post-close, or Sunday; `book_line(ticker, direction)` = ONE annotation line attached to every real-book proposal card in `run_headless` (sandbox-exempt, fail-open) so a newcomer is judged in the context of the existing book. ANNOTATE-ONLY (#63 stage 1): states facts, renders no verdict — #68 keeps the blocking monopoly and runs before it. CLI `python3 -m src.book_context`. |
| `src/performance.py` | Decision #72: risk-adjusted track-record metrics — the "reward function before the RL agent" (2026-07-15 RL discussion). Per-trade Sharpe/Sortino (mean R ÷ stdev / downside-dev; None when there's no dispersion, never a fabricated ∞) + max drawdown (peak-to-trough on the cumulative-R and cumulative-rupee curves) + win rate / avg-R / total P&L over the REAL resolved journal trades only (sim corpus excluded; `hypothetical` #31 shadows excluded; rows missing r_multiple skipped). Return unit is the R-multiple the tracker stamps; NOT annualized (per-trade, said on the card). HONEST ABSTENTION below `performance_min_trades` (default 20) — a Sharpe on 4 trades is theatre (#50, same discipline as the skeptic #44). Advisory/read-only, fail-open. CLI `python3 -m src.performance`; weekly Saturday 10:05 cron posts only when it has a verdict (abstain = silent). |
| `src/calibration/mfe_mae_analyzer.py` | MFE/MAE expectancy surface (spec §3.1/§3.2): journal + simulated_trades read-only (mode=ro), one bar-fetch per ticker via SafeDhanClient, winner-based Apex TP/SL suggestion with a 20-trade abstention floor. Advisory only. |
| `src/journal.py` | Appends every approved/rejected decision to `data/journal.jsonl` with signal, risk levers, pattern tags, plan block, outcome. |
| `src/plan_tracker.py` | Resolves OPEN plan-carrying trades against real daily OHLC high/low (stop/target/time-stop) — NOT a naive last-price check. Closes paper positions on resolution (bracket-order semantics). Spread exit costs charge the entry-side slippage ladder ONLY on legs without `fill_basis: "quoted"` (#70 — a quoted fill already crossed the bid-ask at entry; pre-#70 journal rows have no stamp and keep legacy economics). **P1 crisis honesty (2026-07-17):** `apply_slippage(price, instr, vix=None, lots=1)` scales OPTION slippage by `_vix_slippage_mult` (calm<15=1x → ~2x by VIX25 → +0.3x/pt, cap 6x — the bid-ask blowout; deep-OTM wings savaged off the 0.50% base) + a book-depth penalty (~0.05%/10 lots); `_spread_exit_costs(..., vix=)` charges the EXIT crossing at the exit-day VIX. Both activate ONLY when `vix` is supplied — every legacy caller (vix=None,lots=1) is byte-identical (189 tests green). |
| `src/review.py` | Legacy 7-day price-drift scorecard for pre-plan (non-4B) journal entries. |
| `src/tuner.py` | Learning loop: scores resolved BUY archetypes (fresh-cross vs RSI-oversold), writes `data/brain_weights.json`, which `forecast.py` consumes. |
| `src/evolution.py` | Procedural Evolution (`python3 -m src.evolution`, Mac-side, local Ollama only): mines loss clusters from `simulated_trades` (underlying × strategy × VIX band, provenance = journal_refs), deterministic HER-style hindsight buckets, counterfactual win-contrast, then an Analyst→Critic→resolution dialectic (all replies strict-JSON-gated) proposing ONE mutation from the whitelisted `EVOLVABLE_PARAMETERS` registry (VIX gate, risk %, OTM %, profit-take, pre-expiry days — bounds-checked; the LLM never writes code). Survivors are double-backtested via the Phase 7 simulator (baseline vs `override_parameters`, in-memory DBs, cached bars) — RevertOnRegression discards cluster-fixes that degrade global Sharpe/drawdown. Promoted candidates → `candidates/evolution_<ts>.md` (4-section format + deterministic unified diff) + `data/evolution_lineage.json` version tree (v1→v2 per parameter, failed attempts remembered). NOTHING auto-applies — human gatekeeping only. `--refresh-bars-cache` pulls bars/VIX through the VM (Mac holds no token, decision #48). Runs as sleep-phase Task E where Ollama exists; silent skip elsewhere. |
| `src/trade.py` | Interactive terminal paper-trading session (`python -m src.trade`) — the original human-in-the-loop flow. |
| `src/simulator.py` | Phase 7 time-travel simulator: replays history through the REAL proposal+resolution pipeline via as-of-date injection (decision #36); results land idempotently in `simulated_trades` + outcomes/events/links. CLI fetches daily bars AND India VIX history (`_fetch_vix_series` — so the VIX gate and stored rows see true readings; `--no-vix` to skip). §5.5 T+1-open contract (decision #65): `run_simulation(eod_signal_days=…, eod_signal_layer=…, opens_by_date=…)` — EOD-signal proposals decide on day-T data but EXECUTE at T+1's true open (gapped chain spot; refusal, never interpolation, when no open exists), rows carry additive `signal_day`/`signal_age_hours`/`entry_basis`. Run: `python3 -m src.simulator --start … --end …`. |
| `src/execution_timing.py` | §5.5 (decision #65): the single home of EOD-signal execution arithmetic — `EOD_PUBLICATION_IST` (when each artifact actually exists), `signal_age_hours` (weekend-honest staleness at fill), `next_trading_bar` (bars ARE the trading calendar), `t1_open_entry` (fill at the next bar's true open from an {date: open} map; refuses without one). Consumed by the simulator now, built for the Phase-5 walk-forward miners. |
| `src/skeptic_agent.py` | Phase 11 Random Forest Skeptic: `RandomForestAuditor` builds the frozen `FEATURE_NAMES` vector (graph context + market numbers) and, once `data/skeptic_model.pkl` exists, emits P(win) with a ⚠️ warning below 0.40. ABSTAINS (None, no warning) without a trained model. Advisory only, fail-safe, no market-data imports. |
| `src/train_skeptic.py` | Phase 7b trainer: fits the skeptic's forest on `simulated_trades` (frozen feature order, scratch dropped, class-balanced), evaluates on a stratified holdout, and persists `data/skeptic_model.pkl` + meta sidecar ONLY above the `MIN_BALANCED_ACCURACY` 0.60 ship gate (decision #44 — a noise model must never ship; `--force` for experiments). Run: `python3 -m src.train_skeptic [--dry-run]`. |

## Analysis (top-down sectoral)

Department 8 (the research desk). Read-only, strict point-in-time, NULL-honest,
fail-open; the one live seam out is `regime_filters.advise()`. The review #2
zero-coverage debt is CLOSED (2026-07-19): `tests/test_regime_filters.py`
covers the manager seam + proposer contract, `tests/test_analysis_signals.py`
covers all five signal/research modules.

| File | Purpose |
|---|---|
| `src/analysis/annual_report_analyzer.py` | The 'Harshad Mehta' forensic pipeline (Dept 8, 2026-07-19, advisory-only): annual-report PDF → strict evidence-backed conviction JSON at `data/lake/fundamental_reports/<TICKER>/<FY>.json`. Stages: pypdf page extraction (lazy import, Mac-side dep NOT in requirements; unreadable pages counted never guessed; NUL bytes stripped from extracted text — a corrupted font/encoding table on some small-cap filings makes pypdf emit them, and a single NUL anywhere in the corpus file makes grep and other line-based text tools silently treat it as binary and match nothing, 2026-07-18 incident) → section-aware condensation (regex headers + ligature normalization — PDFs render 'ﬁnancial'; section hit BEATS the boilerplate drop; per-section budget shares + unused-budget rollover; pages ranked by weighted FORENSIC density where transactional phrases — loans-to, interest rate, brand fee, guarantees — count 4x; **measured needle recall 28/29 (97%) against hand-verified pages across 6 real reports** (large-caps + the AZAD/JWL/EMUDHRA small-cap benchmarks; budget 480k chosen at the measured plateau); quality-of-earnings vocabulary weighted heavy — capitali[sz]ed costs, unbilled revenue, order book, the post-2023 audit-trail exception; known permanent residual: a needle on a low-vocabulary page — VEDL FY25 p291 — evades lexical ranking at any budget) → ~8k-char chunks through `text_intelligence.get_extractor()` (#74, the ONE LLM door; ollama 3B today, claude by config — small chunks because Ollama's default context silently truncates) → **the verbatim-quote validator**: every finding's quote must appear on the cited page ±1 (ligature/whitespace-normalized) or it is DROPPED and counted — a weak model degrades to fewer findings, never fake ones → v1.2 REPORT-LEVEL REDUCTION (the measured v1.1 lesson: a per-chunk cap can't reach analyst selectivity — ~2 findings × ~68 chunks ≈ 136/report; `reduce_findings` dedupes overlap-repeated quotes and keeps the top 10 per category by forensic weight of the quote itself — eMudhra measured 61→10 flags with unbilled-revenue ranked #1) → mechanical_v1.2 score on the reduced counts (flags 2x wins; deliberately NOT retuned to flatter the 3B — a 10-flag/0-win report scoring 0.0 is the formula working). v1.1 prompt = Senior Forensic Auditor persona + MAX-2-findings-per-chunk materiality cap + no-boilerplate rule; per-chunk gc + post-report `extractor.unload()` (best-effort) keep the 8GB Mac flat between documents. Both directions reported: operational_wins + shareholder_returns + hidden_risks_and_flags. CLI `python3 -m src.analysis.annual_report_analyzer <pdf> --ticker T --fy FYnn [--dry-run]`. |
| `scripts/model_benchmarker.py` | Dept 8 judgment-stage model A/B bench (2026-07-19, Mac operator tool — SOLO runs only, the contention lesson baked into its docstring): runs candidate local models sequentially through the REAL pipeline (`analyze` — same condenser/prompt/validator/v1.2 reduction, model is the only variable) on one report; pulls a missing model, unloads the previous one before and the candidate after (8GB rule — two resident models is a swap sentence; refuses nothing ≤~2.5GB, the 8B stays refused); per-model metrics (solo minutes, raw findings, hallucinations dropped, kept, score, failed chunks) + the two needle booleans (p154 caught, crisp ₹476 Mn quote); outputs quarantined under `fundamental_reports_auto_bench/<model>/`; writes the side-by-side `model_matchup.md` incl. the human-analyst and prior-llama reference rows (scales never merged). One broken model never kills the bench. CLI `python3 -m scripts.model_benchmarker --models phi3:mini qwen2.5:3b-instruct`. Pure parts tested in `tests/test_model_benchmarker.py` (3, offline). |
| `src/analysis/patience_basket.py` | The Mac EOD evening chain ORCHESTRATOR (Dept 8; the RIPE bucket logic that lived here until 2026-07-20 is SCRAPPED — grading moved to `darling_tiers.py` under the owner's Lifecycle directive): `eod_chain()` = today's bhavcopy → F&O bundle → pricer → valuation → tier grading → darling shadow leg (tier-driven entries + Strong-Sell forced exits), each stage fail-open; a holiday still re-grades from existing artifacts. Honors the weekly clock's pins through the tier engine. **ANALYSIS-ONLY since decision #83** — the chain no longer trades, funds, or rotates anything; its last stage ships the three artifacts (tier table, pricer levels, weekly darling ids via `scrip_master.ensure_darling_ids`) down `firm_treasury.vm_push_file` to the VM, whose live desk + 19:50 treasury cron consume them behind their own freshness gates. Fail-open per artifact. (History — the #79/#80/#82 era wired capital seams, treasury rotation and snapshot push here): `equity_desk.fund_entry`/`settle_exit` injected into `run_darling_cycle` when `equity_desk_enabled`, one desk card per run via `broadcast_activity`; a missing/disabled desk degrades to zero-capital telemetry, never a broken chain. Mac-only (boundary doctrine); cron line pasted by the owner (TCC). **MACRO LEG (07-23, M4):** the chain's last stage now runs macro_lake.ingest_all → indices_lake.ingest_day → macro_regime.declare (fail-open — a dead macro leg leaves tonight's ledger honest-empty, never breaks the darlings chain), so the 60-session public scoring clock rides the EXISTING owner-pasted cron line. CLI `python3 -m src.analysis.patience_basket [--eod] [--dry-run]` (no flag: re-grade only, no fetches). |
| `src/analysis/darling_tiers.py` | The 7-Tier Darling grading engine (Dept 8, 2026-07-20 — the Lifecycle Portfolio Management System, RIPE terminology scrapped by owner directive): every EOD the whole cohort (queue ∪ pinned holds) lands in exactly one tier — strong_buy (in zone + val≤25 + clean + not overextended), weak_buy (in-or-near zone ≤5% + val≤45), strong_hold (close>50DMA>200DMA), weak_hold (val 70-84 OR within 1 ATR of the stop reference [trailing pivot floor first] OR the honest fallback), weak_sell (zone break OR losing volume [20d avg turnover<60d] + val≥70), strong_sell (val≥85 OR close<hard stop OR PINNED by the weekly re-screen), watch (forensic caution band — never mechanically a Buy; the absurd-discount override is a HUMAN call, facts recorded), ungraded (TIER 0 NULL-honesty: missing levels/vetoed valuation, a grade is never manufactured). First matching rule wins, every row records WHICH rule fired. PINS (`data/darling_pins.json`, weekly_recalibration writes): rejected→strong_sell, data-lost→ungraded; pin holds a dropped name in the table until its shadow closes (No-Orphan rule), then the daily run clears it. CARDS: family transitions ONLY (buy/hold/sell/watch; intrafamily silent; first grading = ONE distribution summary); the tier file `data/darling_tiers.json` IS the de-dup ledger. ADVISORY-ONLY (Law #63), zero-capital. CLI `python3 -m src.analysis.darling_tiers [--dry-run]`. |
| `src/analysis/weekly_recalibration.py` | The Saturday fundamental clock (Dept 8, 2026-07-20 — the weekly half of the Two-Clock architecture; fundamentals only change on filings, so weekly is their honest cadence): (1) refresh quarterly filings for every captured symbol (`integrated_results`, Mac-only, throttled — the long stage), (2) re-run `fundamental_screener` → NEW darlings queue (drops failures, admits passers; new passers still ripen through the forensic pipeline), (3) No-Orphan pins — dropped+held → pinned (strong_sell when REJECTED, ungraded when data merely went missing; a sell verdict is never manufactured from absence), dropped+unheld → plain drop, re-passed → un-pinned, carried pins keep their original story, (4) rebuild pricer → valuation (fresh μ/σ) → tiers so Monday opens ruthlessly current — the weekly clock overrides the daily through the pins, (5) ONE Discord summary card. Every stage injectable + fail-open (a refresh outage re-screens the existing lake). CLI `python3 -m src.analysis.weekly_recalibration [--dry-run] [--skip-refresh]`. |
| `src/analysis/valuation_scorer.py` | Valuation Normalization Engine (Dept 8, 2026-07-19, owner handoff spec + amendments; ADVISORY-ONLY, Mac-only): z-scores vs winsorized (1% trim) sector-or-market stats → weighted composite → sigmoid → 1-100 int (1=deeply undervalued, spec directionality). V1 metrics from exchange-filed data only — P/E .40 (close/TTM filed EPS), PEG .30, P/S .30 (mcap via filed paid-up capital ÷ face value; the spec's P/B + FCF legs PENDING a balance-sheet source, D/E dropped by owner amendment, EV/EBITDA blocked on net debt). Sector stats need ≥20 members (sector_universe.json map) else market-wide over the 228-name captured universe (the honest Nifty-500 stand-in). Owner edge-case amendments: negative TTM EPS / non-positive growth → instant VETO never capped; missing data → insufficient_data never guessed. Weights await Dept-5 forward evidence (spec's backtest protocol deferred, not skipped silently). Writes `data/darlings_valuation.json` (enrichment beside the queue — the queue file stays the downloader's untouched contract). CLI `python3 -m src.analysis.valuation_scorer [--dry-run]`. |
| `src/analysis/dynamic_pricer.py` | Dynamic levels for the Darlings (Dept 8, 2026-07-19, ADVISORY-ONLY — Law #63 twice reaffirmed; Mac-side per the boundary doctrine, only its output artifact ships): per queued darling from bhavcopy bars — BUY ZONE = anchored VWAP (highest-volume up-day of last 60 sessions) widened to the nearest high-volume node band (daily-resolution HVN = the honest volume-profile stand-in); STOP = zone floor − 1.5×ATR(14) (volatility widens it, no static %); TRIMS = confirmed swing-pivot highs, trailing floor = latest confirmed pivot low (±5-bar flanks, unconfirmed bars never guessed); 50/200-DMA + Law-3 overextension state (>3 ATR above 50-DMA AND >20% above 200-DMA; missing inputs → honest abstain). NULL-honest throughout (<60 sessions → insufficient_history). Writes `data/darlings_levels.json` + one shadow-journal line per symbol to `data/lake/pricer_journal.jsonl` (Dept 5's future entry-timing evidence). CLI `python3 -m src.analysis.dynamic_pricer [--dry-run]`. |
| `src/analysis/equity_entry_checks.py` | The equity shadow path's composed halt stack (2026-07-19, amended mandate: shadow-first, Law #63 stands; SEPARATE list — darling logic never pollutes the index ENTRY_HALT_CHECKS): never_short_darling (non-negotiable, first in stack) → liquidity_filter (WIRED 2026-07-20 to fo_liquidity.json: tier1-only, exchange-ban block, 7-day freshness or fail-closed) → expiry_week_halt (physical-settlement defense: no new equity-option entries within 7 days of the monthly last-Thursday expiry) → overextension_halt (Law 3: no fresh delivery buys while the pricer marks `overextended`; honest abstain does NOT block). `check_entry(proposal)` walks the stack, first block wins. **The 1R gate is PARKED by owner order and deliberately absent (test-enforced).** Consumed by `equity_shadow_proposer.propose_darling_entries` (the darling shadow leg, wired 2026-07-20). |
| `src/analysis/fundamental_screener.py` | The Darling screen (Dept 8, 2026-07-19, advisory — Phase 1 of the owner's Darling Pipeline): mechanical v1 pass rule over exchange-filed numbers — revenue up YoY (banks: interest earned) + net profit > 0 in ALL latest 4 quarters + EPS not shrinking YoY — then the FORENSIC TRUST GATE where a citation-clean deep-read exists (score<45 or ≥2 red flags → OUT; 45-60 → flagged-but-queued; Issue-19 contaminated originals never read, .v2 preferred). NULL-honest: missing data → `insufficient_data`, never a guessed pass. Writes ALL passers (owner rule, no top-N) to `data/darlings_queue.json` ({'tickers':[...]} = the report_downloader contract + full per-symbol detail). First live screen 2026-07-19 (10 names): CUMMINSIND/DIVISLAB/HAL/TCS pass, EMUDHRA flagged (55), TATAPOWER rejected BY THE FORENSIC GATE (score 42, 2 reds — the lie-detector vetoing a quant candidate, the design working), ICICIBANK honest-insufficient (bank criteria = deferred v1 gap). CLI `python3 -m src.analysis.fundamental_screener [--dry-run]`. |
| `src/analysis/cohort_comparator.py` | Cross-sectional research matrix (Dept 8, 2026-07-19): merges every conviction JSON in the lake — human benchmarks (`fundamental_reports/`, analyst 0-100 scale) AND pipeline outputs (`fundamental_reports_auto/`, mechanical 0-1) — into one side-by-side markdown table (`cohort_matrix.md`); the two score scales are NEVER merged or rescaled (different instruments — analyst judgment vs validated-finding counts). `--broadcast` fires ONE summary card via `fire_broadcast`. Deliberately NO brain_map write (review #2 quarantine: research enters the options engine's memory only via explicit mode-tagged ingest after Dept 5). Missing/unreadable files skipped honestly. CLI `python3 -m src.analysis.cohort_comparator [--broadcast] [--json]`. |
| `src/analysis/macro_features.py` | THE macro featurizer (Dept 8, 2026-07-23, Macro Regime Engine M1/M2 seam — Fable-worker build, PM-accepted): historical fingerprints AND tonight's state read share ONE `_vector_at` core (no train/serve skew by construction). `read_series` (lake CSVs, dup dates last-wins), `align` (union calendar, ffill capped at 2 consecutive sessions, every filled cell FLAGGED), `zdelta` (window % change z vs trailing 252 usable changes; insufficient history/degenerate std → None, never a guess), `corr_state`/`_corr_at` (rolling Pearson, None across holes), **the versioned clash definition** `clash_condition` (corr60 < −0.4 AND both |z20| > 1 — the owner's "Dollar-vs-Crude clash" as a measurable constant set), `feature_vector` (as-of state + NAMED holes), `trajectory` (episode fingerprint T−20…T+120, weekend anchors snap to last session, out-of-history offsets honest None). Pure stdlib, zero network, zero writes. Canonical roster includes INDIAVIX/NIFTY ahead of the M1-2 indices leg. |
| `src/analysis/macro_fingerprints.py` | M2 — fingerprints, DTW, archetypes (Dept 8, 2026-07-23, Macro Regime Engine): episodes (`config/macro_episodes.yaml`, malformed rows REFUSED) → fingerprint channel matrices through the ONE featurizer (`macro_features.trajectory`; channels = per-series z20 + dxy_brent_corr60) → banded multivariate DTW (Sakoe-Chiba 20 — phases stretch, never teleport; local cost = mean |Δ| over SHARED channels only; dark cells cross at the versioned 3.0 penalty and drop the recorded coverage; zero shared observation anywhere = distance None, "not comparable" is an answer) → deterministic average-linkage clustering HARD-CAPPED at `K_MAX=4` (None pairs can never merge — honest cap overflow allowed) → `data/macro_templates.json` (atomic; per-pair dtw+coverage, exclusions NAMED). First real build 2026-07-23 (17/17 episodes, 28s): A1 global-financial-risk-off (medoid us_downgrade_2011, 8 members incl. Lehman/COVID/SVB), A2 INR-transmission shocks (medoid taper_tantrum — Ukraine landed HERE not with the financial family, the crude+INR fingerprint dominating its "geopolitical" class hint), A3 pre-2006 thin-channel pair (9/11 + 2004 election, DXY honestly dark), A4 ilfs_nbfc_crisis ALONE (the India-idiosyncratic credit shock matched nothing global — correctly). CLI `python3 -m src.analysis.macro_fingerprints [--dry-run]`. **M2.1 (07-23): the taxonomy layer clusters on CORE channels only (per-horizon commensurable set); India channels = the artifact's `india_view` within-family refinement — the 05:00 coverage-reshuffle pathology is a regression test now. HORIZON CLASSES (owner directive 07-23): `shock` (T−20..T+120, z20, band 20, k≤4, ids A*) vs `slow_burn` (T−60..T+500, z60, band 75, k≤2, ids S*— El Niño cycles, rate grinds); horizons NEVER cross-compare (a war and a weather cycle are different species); artifact is horizon-keyed. Stable build: A1 global-risk-off / A2 INR-transmission / A3 pre-2006 thin / A4 IL&FS alone + S1 el_nino_2023+rbi_hikes / S2 el_nino_2014_16 (solo).** |
| `src/analysis/macro_playbooks.py` | M3 — what the shapes actually pay (Dept 8, 2026-07-23): per (archetype, phase, sector) — every NIFTY sector index's absolute AND excess-vs-NIFTY return after that archetype's episodes, n / median / min / max / hit-rate STATED per cell. **HORIZON-AWARE clocks (owner directive): shock P1 0-10 / P2 11-45 / P3 46-120 sessions; slow_burn S1 0-120 / S2 121-300 / S3 301-500 — an El Niño on a 30-day clock is noise, a panic on a 2-year clock is mush; `PHASES_BY_HORIZON` is THE single phase source (macro_regime imports it).** Fixed-window v1 (spec's changepoint segmentation = later refinement, documented). Window edges snap INWARD on the benchmark's date range (a hole at an edge never voids a window); sector history starts 2019-10 → older episodes carry NO sector legs (named in `episodes_without_sector_legs`, never fabricated); n=0 cells don't exist. First real build: A1 P1 leaders PHARMA/FMCG — the hand-written War Playbook independently rediscovered from data. Writes `data/macro_playbooks.json` (atomic). ADVISORY VALUE ZERO until Dept-5 scoring. CLI `python3 -m src.analysis.macro_playbooks [--dry-run]`. |
| `src/analysis/macro_regime.py` | M4 — the nightly declaration tracker (Dept 8, 2026-07-23; spec §3): the trailing window per horizon (shock 60 sessions / slow_burn 250) through THE featurizer → subsequence-DTW against every episode fingerprint (per-horizon stride+band; best window center = "sessions since anchor" → the playbook phase) → DECLARED only when best similarity ≥0.70 AND same-archetype analogs ≥3 (slow_burn ≥2 — a 3-episode catalog demanding 3 analogs is a dead switch, not abstention); runner-up ALWAYS shown; similarity = 1/(1+dtw). Appends ONE line per run to the immutable declaration ledger `logs/macro_regime_declarations.jsonl` (Dept 5 scores the LEDGER, never a self-backtest) + writes `data/macro_regime.json` + ONE Discord card on FAMILY TRANSITIONS only (sameness silent; the transition detector reads both doc shapes; card failure fail-open). ZERO execution authority until stat_gates pass the ledger. First declarations 07-23: shock A2 P3 (0.740, analog demonetization), slow_burn S1 S2_peak_effect (0.775). Wired as the EOD chain's macro leg (patience_basket). CLI `python3 -m src.analysis.macro_regime [--dry-run]`. |
| `src/analysis/macro_nightly.py` | The VM macro heartbeat (Dept 8, 2026-07-23) — the always-on daily run that makes the 60-session scoring clock REAL. VM cron (19:50 IST), NOT the Mac EOD chain (a clock that only ticks when a laptop is open is not a clock). **DUMB-EXECUTOR fail-fast: calls `declare(require_cache=True)` — a stale/absent fingerprint cache ABSTAINS with a named `cache_miss_*` reason + a loud `Cache Miss/Stale - Aborting` heartbeat ALERT, NEVER the 30-min e2-micro recompute (the silence-ban fix after the cache-leak incident); every declare stamps `cache_status`.** Three fail-open stages: ingest FRED globals (`macro_lake.ingest_all`, needs `FRED_API_KEY` in VM env) → ingest today's NSE indices (`indices_lake.ingest_day`, static archive/scripted-safe) → `macro_regime.declare` onto the immutable ledger. Does NOT rebuild templates/playbooks (catalog-change only, rebuilt on the Mac + shipped). **DUMB-EXECUTOR fail-fast (07-23): calls `declare(require_cache=True)` — a stale/absent fingerprint cache ABSTAINS with a named `cache_miss_*` reason + loud `Cache Miss/Stale - Aborting` heartbeat ALERT, NEVER the 30-min e2-micro recompute; every declare stamps `cache_status`.** One heartbeat line → `logs/macro_nightly.log`; every stage caught independently so a dead source never aborts the cron. Injectable (`fred_fn`/`indices_fn`/`declare_fn`) for offline tests. Requires the Mac-built macro lake + templates + playbooks seeded to the VM once (they're gitignored). CLI/cron `python3 -m src.analysis.macro_nightly`. |
| `src/analysis/auto_discovery.py` | The unsupervised discovery engine (Dept 8, 2026-07-23; docs/auto_discovery_spec.md) — proposes its OWN episodes from 25y of cross-asset data, no human `macro_episodes.yaml` input; human labels become post-hoc annotations, never inputs. **AD-1 FUNCTIONAL:** shock scan (`rank_shock_candidates` — per-day system-stress = RMS of |z20| across core global channels, local peaks de-duped by a 90-day min-gap so one crisis = one anchor) + slow-burn motif scan (`scan_motifs` — sliding ~18mo z60 windows, lowest-DTW self-similar NON-overlapping pairs, reusing the ONE `macro_fingerprints.dtw_distance`). Writes `data/discovered_candidates.json`, ZERO authority. **AD-2 LIVE (the significance layer / moat):** `significance_gate` admits a shock candidate only if it beats BOTH nulls — `block_bootstrap` (block-resampled) AND `phase_randomize` (pure-python radix-2 FFT, spectrum-preserving) at alpha — AND `held_out_confirms` (OOS: a train-head shock must recur in the held-out tail). Pure-stdlib surrogates; noise is rejected (abstains, never hallucinates). **AD-3 LIVE:** `route_to_court` writes AD-2-admitted candidates to `data/discovered_episodes.json` (source=auto; full Dept-5 registry enrolment is the remaining slice). **AD-4 LIVE:** `merged_catalog` unions human `macro_episodes.yaml` ∪ auto, flagging `discovery=True` for auto regimes far from any human anchor (the card-worthy find). Motif significance (DTW-statistic surrogate test) is the next slice. Stdlib-only, reuses macro_features (baseline resolves at call time — tests shrink it) + the ONE DTW; Mac-side weekly mining job, never the nightly cron. CLI `python3 -m src.analysis.auto_discovery [--dry-run]`. |
| `src/analysis/liquidity_rank.py` | The objective universe ordering (Dept 8, 2026-07-19): ranks every NSE EQ/BE symbol by TRADING LIQUIDITY — average daily traded value (`TURNOVER_LACS`) across the `data/lake/bhavcopy/` history the `bhavcopy_clerk` drops. Liquidity is a factual market attribute, NOT a return/valuation prediction, so it is a safe, honest way to order a scan universe (read the most-traded names first, walk down). Turnover not volume (price-normalised, so a penny stock printing huge share counts can't outrank a real large-cap); tie-breaks on trade count. NULL-honest: NSE's ' -' is skipped, never counted as a zero-turnover day. Measured 2026-07-19: 2,785 rankable symbols over 218 days, #1 HDFCBANK ₹2,790 cr/day. CLI `python3 -m src.analysis.liquidity_rank [--top N] [--from-rank K] [--json OUT]`. |
| `src/analysis/business_metrics.py` | The 'darling' reader (Dept 8, 2026-07-19) — the SECOND report produced from every annual report, deliberately SEPARATE from the forensic pipeline so that proven path is never disturbed (owner directive: "ek forensic ek dusri, forensic me ungli na ho"). Imports `extract_pages`/`validate_findings`/`_clean` from `annual_report_analyzer` but MODIFIES NOTHING there; own sections (ratios/segment/mda/directors/ops), own vocabulary (`BUSINESS_DENSITY`/`BUSINESS_HEAVY`), own budget, own lake at `data/lake/business_metrics/<T>/<FY>.json`. Hunts what no screener has: the **Schedule III ratio table** (mandatory + standardised in every Indian AR since FY22 → directly comparable across companies, with prior year AND management's compulsory explanation of any >25% move; detected by ≥3 standard labels and given a +40 density bonus so it can never be dropped), order book/backlog, capital commitments, capacity/volumes, credit rating + upgrades, customer/segment concentration, mix shift, R&D intensity, guidance. SAME verbatim-quote discipline as forensic (no quote → no field); missing stays MISSING never 0 (a bank has no order book); derived figures marked `derived: true`. `extract_pages` runs ONCE and both condensers consume it — no extra download. CLI `python3 -m src.analysis.business_metrics <pdf> --ticker T --fy FY26 [--dry-run]`. |
| `src/analysis/ticker_dossier.py` | The ANTI-SILO lookup (Dept 8, 2026-07-19): one command answers "everything this desk knows about <TICKER>", reading across EVERY store so research can't hide in folders nobody knows exist — forensic verdicts (all vintages), business/darling metrics, quarantined machine-tier output, source PDFs, yfinance captures, liquidity rank, and the scan-pipeline standing (queued / refresh-queued / unavailable-at-source). Computes nothing and writes nothing. NULL-honest and careful to distinguish "not computed" (`--no-rank`) from "genuinely absent" — a skipped rank must never read as illiquid. CLI `python3 -m src.analysis.ticker_dossier TICKER [--json] [--no-rank]`. |
| `src/analysis/conviction.py` | Multi-Factor Conviction Engine (2026-07-16, research-stage ORPHAN — imported by nothing, no tests; indexed in review #2 after shipping unindexed in `6d89eb4`). Normalized [0,1] conviction score per ticker/date: Smart-Money footprint 40% (net institutional flow via `smart_money_trend`) + Fundamental quality 40% (ROE, Debt/Eq, CFO/PAT cash-is-king; cash-negative value-trap crushed ×0.25) + Macro/Sector tailwind 20% (top-3 leader in an outperforming sector). `CONVICTION_VETO=0.40` floor; NULL-honest (missing factor → neutral 0.5, never a fabricated edge); pure factor functions + `score_from_inputs` seam for the sim. **Its docstring's ambition — driving dynamic position sizing — is a Dept 3 power it does NOT have: sizing authority requires Dept 5 validation first, then wiring through `portfolio_manager` (ARCHITECTURE.md, Dept 8 rules).** |
| `src/analysis/institutional_alpha.py` | Smart-Money Accumulation VWAP-pullback entry primitives (2026-07-16, research-stage — no live importer, no tests; indexed in review #2 after shipping unindexed in `6d89eb4`). `accumulation(deals, as_of, 45d)`: heavy multi-deal net institutional BUYING (≥2 buy deals, net/gross ≥0.20) + the buy-VWAP "defense line"; `pullback_trigger`: price pulls back FROM ABOVE, tests the VWAP intraday, daily close holds above the 4% invalidation band → buy at the institutions' average. Strict point-in-time (deals strictly before decision day). Conceptual sibling of the Shadow Equity Engine's entry thesis (`equity_shadow_proposer` implements its own inline variant); backtest resolution lives in the sandbox. |
| `src/analysis/macro_shocks.py` | Crisis-Regime ('War/Shock') Playbook (2026-07-16). Hardcoded major shock windows (2014 Crimea, 2019 Balakot, 2020 COVID, 2022 Russia-Ukraine, 2023 Israel-Hamas); maps each sector index's window return, drawdown, and EXCESS vs NIFTY, aggregating to a favoured/avoid ranking. Findings: FAVOUR Energy/Pharma/FMCG/Metal, AVOID IT/Financials/Auto in crises (regime-type-dependent: commodity shocks→Energy/Metal, demand shocks→Pharma/FMCG). `active_shock(as_of)` = point-in-time crisis switch. Read-only, NOT wired in. CLI `python3 -m src.analysis.macro_shocks`. |
| `src/analysis/regime_filters.py` | Advisory regime radars wired into the OPTIONS engine (2026-07-17 pivot; the vol_bridge pattern — composed in `market_loop.fetch_market_state`, honored in `options_proposer.build_proposal` via the additive `advisory=` kwarg; FAIL-OPEN throughout so absent data = proposer unchanged, all 56 existing options tests still green). (1) Smart-money/sector VETO: before a BULLISH index spread, if >=2 of the index's top-3 heavyweights (`INDEX_CONSTITUENTS`) show net institutional DISTRIBUTION (`smart_money_trend`) OR the parent sector is not bullish (`sector_trend`), veto it. (2) War Playbook CRISIS regime (`crisis_regime`): VIX>=25 panic OR abrupt VIX spike OR a known `macro_shocks` window -> disable SHORT-premium (iron condor) so only defined-risk long-premium debit spreads ride the tail. Read-only advisory; nothing places/blocks a trade itself beyond the proposer honoring the verdict. |
| `src/analysis/smart_money_trend.py` | Smart-money (bulk/block deal) accumulation/distribution signals SCAFFOLD (owner directive 2026-07-16). READS the existing `data/deals_history.jsonl` ledger via `deals_tracker.read_deal_history` (NO redundant fetcher — deals_tracker already crawls NSE). `net_institutional_volume(ticker, as_of, 90d)` = value-weighted net buy−sell (accumulation/distribution); `block_deal_vwap(ticker, as_of, 6mo)` = VWAP of major BLOCK buys (smart-money floor); `smart_money_ok` = Gate-5 verdict (accumulation OR price>block-VWAP; None=honest abstain when no deals). Retail-noise handling = value-weighting + block-only VWAP (not a name classifier). STRICT point-in-time (deals as_of STRICTLY < decision day). Read-only, NOT wired in. Sim finding: as a distribution VETO it helps (Avg R +0.48→+0.59); as a required confirmation it hurts (data-sparse). CLI `python3 -m src.analysis.smart_money_trend`. |
| `src/analysis/sector_trend.py` | Top-Down sector trend + relative strength SCAFFOLD (owner directive 2026-07-16 — a strong stock still fails in a weak industry). `is_sector_bullish(sector)` — is the NSE sector INDEX above BOTH its 50-SMA and 200-SMA (the industry "tide")? `get_relative_strength(ticker, sector)` — stock vs parent-index momentum over ~63d → leader/laggard. Reads `config/sector_universe.json` (sector→index+constituents) + `data/sector_index_bars.json` (yfinance-sourced index bars); stock bars injected while the live Dhan path is token-gated. Read-only, NULL-honest (missing data → explicit error, never a guessed verdict), NOT wired into the engine/Compass yet. CLI `python3 -m src.analysis.sector_trend`. |

## Alerting & notifications

| File | Purpose |
|---|---|
| `src/main.py` | Alert entry point (`python -m src.main`) — watchlist -> rule check -> email digest. (NOT the trading-session entry point — that's `src/master_scheduler.py`.) |
| `src/master_scheduler.py` | Phase 7A master scheduler (`python3 -m src.master_scheduler`): one command runs a full automated paper-trading day — waits for the 09:15 IST open, supervises the entry loop (market_loop + live adapter, margin-gated PENDING_APPROVAL proposals) and exit loop (live_bridge advisory alerts) as asyncio tasks, self-terminates at 15:30, graceful SIGINT/SIGTERM shutdown, Discord session bookends (account snapshot + planner playbook). `_shadow_cycle_fn` is the composition root for BOTH equity legs (#83): the block-VWAP telemetry cycle + the darling desk's live cycle (`EQUITY_DESK_ENABLED`-gated), each fail-open so one leg never kills the other or the options engine. Cron instructions: `CRON_SETUP.md`. |
| `src/notifier.py` | Gmail SMTP (`send_digest`), async Discord text push (`send_discord_message`), structured embed broadcaster (`broadcast_alert` async / `fire_broadcast` sync bridge — colour-coded embeds for trade lifecycle events). Phase 6J test-env muzzle: `webhooks_muzzled()` blocks ALL webhook HTTP when `IS_TEST_ENV` is truthy or a pytest run is detected — muzzled sends are logged locally and report False. **THE 5/DAY DISCORD BUDGET (#84 Directive 4):** `budget_gate` inside `broadcast_alert` — `system_crash` ALWAYS pages; scheduled digests (eod/ceo_brief/darling_tiers/digest/performance) send within `discord_daily_budget`; `portfolio_report` drops (data ledgers unaffected); everything else spools to `logs/discord_digest_queue.jsonl` → rendered by `drain_digest_queue` into the next EOD/CEO card's "📦 Batched signals" field (drained rows archived). Delivered cards burn budget, failed posts don't; suppressed sends return True (the digest IS delivery). Kill switch `discord_budget_enabled` (code default OFF). |
| `src/ops_monitor.py` | Nightly ops sweep (`python3 -m src.ops_monitor`, cron 20:30 IST): incrementally scans `logs/*.log` for problem-shaped lines (byte-offset state — each problem reported once), appends every finding to `logs/problems.jsonl`, checks each scheduled job's log was touched today (weekday-aware heartbeats, includes `push_token_to_vm.log`), posts a terse Discord health card. |
| `src/renew_token.py` | Daily DhanHQ token minting (`python3 -m src.renew_token`, cron 07:00 IST on BOTH machines): V2 PIN+TOTP flow, credentials from `.env` when present (the Mac) or fetched at runtime from **GCP Secret Manager** via metadata-server OAuth (the VM — pure stdlib, keys never on disk, decision #47); legacy `/v2/RenewToken` kept only as a loud deprecated fallback. Rewrites ONLY the token line (`.env.bak` kept). |
| `scripts/push_token_to_vm.sh` | Pushes ONLY the freshly-renewed `DHAN_ACCESS_TOKEN` to the VM. **NOT on any automatic schedule** (decision #48: DhanHQ allows one active token per account, so an unattended Mac renewal can invalidate and overwrite the VM's currently-valid token — the "redundancy" was actually a race that could break the live engine). Kept as a manual/dev tool for troubleshooting only. Token crosses via a chmod-600 temp file over `gcloud compute scp` (never embedded in a command line); the VM updates its `.env` via `renew_token.replace_token` and restarts `alpha-trading.service`. `--dry-run` to preview. |
| `src/edge_miner.py` | Opportunistic Mac-side causal mining (decision #47, LaunchAgent at login + 21:00 via `scripts/mine_edges.sh`): guards (Ollama up, gcloud present, >20h since last success) → pulls the VM's `brain_map.db` → runs `sleep_phase.write_causal_links` with local Ollama → replays only the NEW triples through idempotent `graph_engine.add_edge` on the VM (never file overwrite) → refreshes the Mac's read-only `data/` copies for chat_agent (originals archived once at `data/mac-archive-pre-vm/`). `--force` skips the 20h gate. No paid LLM API involved, ever. |
| `src/firm_mtm.py` | #84 Directive 6 (read-only reporting): the firm MTM + return line on the EOD/CEO digests — MTM = realized account equity + options unrealized (portfolio_report's snapshot-first ladder, #56) + funded-darling unrealized (desk live_quote). Base + run-start DERIVED (account starting_capital + the latest `clean_sheet` event) — a future capital reset re-bases automatically. DAY-1 EDGE: below `CAGR_MIN_DAYS` (30) the line shows the honest ABSOLUTE return with a "day N" stamp ("CAGR unlocks at day 30 — annualizing a days-old number would be noise"); from day 30, true CAGR = (MTM/base)^(365/days)−1 beside it. Partial marks stated ("N unmarked — MTM partial"). `market_data_muzzled` guard (equity_desk) keeps un-injected render paths hermetic in test envs. CLI `python3 -m src.firm_mtm`. |
| `src/bug_ledger.py` | #84 Directive 5: the autonomous run's internal bug ledger — nightly (cron #22, 20:40 IST, after the 20:30 ops sweep) folds problems.jsonl lines + silent `account_events` rejections/halts + treasury anomalies + sizing VETOES into `logs/autonomous_bug_report.jsonl` (dedup by source+fingerprint, ledger-as-memory). Read-only inputs, append-only output, imported by NOTHING in the trading path. `--report` = the THURSDAY PROTOCOL's mandatory first read (see HANDOVER.md's top section): analyze + fix every row before any new work. |
| `src/ceo_brief.py` | **The Daily CEO Brief** (Dept 6 reporting; `python3 -m src.ceo_brief [--dry-run]`, cron #19 16:30 IST weekdays): ONE cross-department card answering the owner's four questions — Operations (scheduled-job heartbeats via `ops_monitor.check_heartbeats`), Issues (problem lines bucketed into dead-ticker / margin / token / Dhan-API / other), Deployments (live sha per service from `logs/deploy_log.jsonl` + today's local commits, hostname-stamped), and Risk & Capital (realized P&L + open exposure + net delta, every number REUSED from `eod_summary` — never recomputed, so the 15:30 and 16:00 cards can't disagree). Routes strictly through `notifier.fire_broadcast` (event `ceo_brief`; test-enforced — no `httpx`/`discord_client` import). **Keeps its OWN sweep offset (`logs/.ceo_brief_state.json`) and never writes `problems.jsonl`** — `ops_monitor`'s incremental state is untouched, so the 20:30 nightly card still sees every line (test-enforced). Read-only + fail-open per section (a broken collector costs its own field, not the card). CAVEAT ON THE CARD: `dhan_client._throttle()` paces by sleeping without logging, so Dhan *throttle events* are unobservable and NOT counted — the Dhan bucket reports API errors only; instrumenting the throttle counter is a pending Dept-1 follow-up. |
| `src/eod_summary.py` | Daily EOD broadcaster (`python3 -m src.eod_summary`, **cron #20, Mon-Fri 15:45 IST**): queries journal + brain_map.db for today's MTM P&L, active positions count, and net delta exposure; posts a terse embed card via `broadcast_alert`. Also THE owner of those computations — `ceo_brief` reuses them rather than recomputing, so the two cards can't disagree. **Its docstring claimed 15:30 from the day it was written, but it was never in `setup_cron.sh` and nothing imported it — the card had never fired in production until cron #20 (2026-07-18). 15:45 not 15:30: master_scheduler self-terminates AT 15:30, so a 15:30 read can catch the book mid-shutdown.** No test file yet. |
| `src/config.py` | Loads + validates `config.json` at import time (fails loudly on missing/bad keys) — RSI thresholds, SMA windows, risk levers, tuner params. |

## Interfaces (front doors)

| File | Purpose |
|---|---|
| `src/api.py` | Unified local FastAPI backend: all `/api/*` routes (see `ARCHITECTURE.md`), the hourly auto-sync loop, the Phase 6 web dashboard routes (`GET /dashboard`, `/api/web/positions` via the shared mark ladder, `/api/web/pnl` — realized+open+net P&L strip, `/api/web/departments` — the 7-Manager health strip derived ONLY from `ops_monitor` heartbeats+problem ledger (inline depts show 'inline', never a faked green), `/api/web/events` SSE change-watcher), `ApiKeyMiddleware` (key-optional local mode; accepts X-API-Key / Bearer / `?api_key=` — the query form exists because EventSource can't send headers), env-gated `EXTRA_CORS_ORIGINS`. |
| `src/api_server.py` | Phase 9 strict PUBLIC gateway (the one process the tunnel exposes): mounts the full `src.api` app behind fail-closed auth (503 when API_KEY unset, 401 otherwise; only `/api/health` public) + the two-way Discord bridge `POST /api/discord/action` → `decide_pending`. |
| `src/brain_mcp.py` | The Brain-Map MCP server (2026-07-22, cycle_hunter Phase D prototype — the data PRODUCT's first door, localhost-only until gate G2): hand-rolled newline-delimited JSON-RPC 2.0 stdio MCP server, ZERO new deps (framework-free doctrine). 9 read-only tools over the brain + artifacts — darling_tier_table, valuation_scores, earnings_quality (newest FY extract), event_history (events + linked measured outcomes), entity_affinity, fo_liquidity, market_regime, firm_equity_curve, strategy_stats. Posture is test-enforced: sqlite opens `mode=ro` (cannot write the brain, by construction), no buy/sell/recommend verbs on the tool surface (SEBI: data-not-advice), paper/simulated figures stamped in-band (`basis`), missing artifact/table = honest `available:false`, tool error = `isError` answer never a dead server, malformed lines skipped. Fully injectable (`Sources`, `serve(inp,out)`) — offline tests. PRODUCT TODO at G2: neutralize internal tier labels (strong_buy etc.) at this boundary before an external user sees them. Repo `.mcp.json` wires it into Claude Code for the zero-step demo. CLI `python3 -m src.brain_mcp` (stdio). |
| `src/deploy_log.py` | Startup deploy record — "which update went live when": every service/job appends `{ts, service, kind, sha, subject, dirty, event}` to git-ignored `logs/deploy_log.jsonl` at startup (`event` = "deploy" when the sha changed vs that service's last entry, "restart" otherwise; `dirty` flags uncommitted VM hotpatches). **`kind`** (2026-07-20) separates `"service"` (long-running — its sha IS what's live) from `"job"` (scheduled + self-terminating, e.g. `master_scheduler` 09:10→15:30 — its sha is history); only services are sha-compared for deploy consistency, since comparing an exited job made `ceo_brief` cry "half-finished deploy" on any day a commit landed mid-session. Fail-open, stdlib-only. Wired into `api_server` (lifespan), `discord_bot.run()`, `master_scheduler` (`__main__`, `kind="job"`). Inspect: `python3 -m src.deploy_log`. Cross-reference issues in `docs/observation_week_ledger.md` against this log. |
| `src/discord_client.py` | Outgoing async webhook push (lightweight, no gateway connection) — separate from the interactive `discord_bot.py`. |
| `src/web/static/dashboard.html` | Single-file dark-terminal positions dashboard (Phase 6): SSE-driven, deliberately NO polling timers; reads `?api_key=` off its own URL and propagates it to fetch + EventSource so it works behind the gateway. |
| `src/discord_bot.py` | Discord analyst bot: `/analyze` slash command (forecast), chat replies via Gemini. Read-only on the engine (imports only `forecast.py`). |
| `src/chat_agent.py` | ADiTrader reasoning mirror: local Discord bot (@mention-gated, single authorized user) routing stateless queries to local Ollama with a terse DB context (`fetch_agent_context`). `@ADiTrader portfolio` bypasses the LLM entirely — `build_portfolio_snapshot` formats the Phase 6G account (starting capital, free cash, locked margin, active trades, net P&L) as hard numbers (Phase 6J). Read-only, no execution pathways. |
| `src/web/watchlist_store.py` | Reads/writes `config/watchlist.yaml` (ruamel round-trip, preserves comments); ticker validation goes through `dhan_client`. |
| `src/web/static/index.html` | Legacy static dashboard HTML, served by `src/api.py`'s `GET /`. Superseded by `lovable-frontend/` for active development. |
| `lovable-frontend/` | React (TanStack Start + Vite) dashboard. **Gitignored on `main`** — lives only on the `lovable-ui` branch. See `DATA_CONTRACT.md`. |

## Config & data files (not code)

| File | Purpose |
|---|---|
| `config.json` | Non-secret tunables: RSI/SMA thresholds, risk levers, tuner params. Required keys enforced by `src/config.py`. |
| `config/watchlist.yaml` | The watchlist: tickers, type (stock/index), optional alert rules. Expanded 2026-07-16 to 76 tickers (28 → +48 sector-universe names). |
| `config/sector_universe.json` | Top-Down Sectoral map (2026-07-16): 7 NSE sectors (IT/FINANCIALS/PHARMA/AUTO/FMCG/METAL/ENERGY) → parent index (Yahoo `^CNX*`/`^NSEBANK`) + ~10 constituents each (70 tickers). Source of truth for `src/analysis/sector_trend.py`. |
| `data/sector_index_bars.json` | 7 sectoral-index daily bars (yfinance, 10+ yrs), `{yahoo_index: {sector, bars:[[date,low,high,close],…]}}`. Git-ignored; read by `sector_trend`. |
| `.env` / `.env.example` | Secrets (git-ignored) / safe placeholder template (versioned). Keys: `DHAN_CLIENT_ID`, `DHAN_ACCESS_TOKEN`, `GEMINI_API_KEY`, `DISCORD_BOT_TOKEN`, `ALERT_EMAIL_*`. |
| `data/portfolio.json` | Paper portfolio (cash + holdings). Git-ignored, personal, live. |
| `data/journal.jsonl` | Every trade decision ever logged (one JSON object per line). Git-ignored, personal, live. |
| `data/news_sentiment.json` | Latest Gemini-scored news sentiment per ticker. Git-ignored (regenerable). |
| `data/brain_weights.json` | Tuner's learned per-archetype weights. Git-ignored (regenerable). |

## Tests

| File | Purpose |
|---|---|
| `tests/test_notifier.py` | `broadcast_alert` embed dispatch, `fire_broadcast` sync bridge, EOD card builder, `query_todays_resolutions`, `compute_net_delta_exposure` — 53 tests, offline, pytest-mock. |
| `tests/test_vol_bridge.py` | Phase 6F vol bridge: polarity classification, net-signal arithmetic, regime boundary precision, macro shock scenarios, scale_risk/widen_wings parameter scaling, run_headless integration — 31 tests, offline. |
| `tests/test_exposure_gate.py` | Decision #68 suite: gate verdicts (duplicate blocked / other direction-underlying allowed / neutral slot / pending-rejected-resolved never block / legacy fallback / fail-open), constructor anti-drift lock, ledger + once-per-day Discord note, run_headless integration (margin gate never reached on a block, auto-approve path gated, sandbox spy), flip advisory (fire-once/refire, restart-into-contradicted-book, condor-silent, zero-fetch when idle, session cache, fail-open), live_cycle no-op/de-dup/crash-survival — 25 tests, offline. |
| `tests/test_live_bridge.py` | Phase 6H live bridge: packet parsing, candle-bucket playback, live fetch_market_state contract (hours gate, dead quote, vol_overrides), intraday profit-take/pre-expiry/clamp arithmetic, alert de-dup, read-only sandbox spy — 19 tests, offline. |
| `tests/test_trade_planner.py` | Phase 6I planner: routing matrix, trend/IV classifier boundaries, Bank Nifty strike snapping, support/resistance overrides, VIX-gate consistency, purity + import guard — 21 tests, offline. |
| `tests/test_train_skeptic.py` | Phase 7b trainer: frozen feature-order contract, scratch dropping, thin-data refusals, separable-pattern learning, ship-gate refusal of coin-flip models + `--force` override, trained pickle waking the skeptic from abstain — 12 tests, offline (temp-dir model files only). |
| `tests/test_annual_report_analyzer.py` | The analyzer vs its GROUND TRUTH, two tiers: synthetic (always run — capitalized-R&D/unbilled-revenue pages outrank filler, the audit-trail CARO page tags `auditor` with no header, revenue-recognition KAM vocabulary) + benchmark-corpus (Mac-only, skips clean where the Desktop PDFs/lake benchmarks are absent: the condenser must keep EVERY needle page the human deep-reads of AZAD FY25 / JWL FY25 / EMUDHRA FY26 cited — pages read dynamically from the benchmark JSONs — plus the owner-named pages 12/14, 10/186, 154; 16/16 when written). Analyst scores (72/64/55) deliberately NOT asserted (0-100 human scale vs the mechanical 0-1 — category error). Cohort comparator: both-schema merge without rescaling, one-card broadcast + optional, honest empty dirs. v1.1/v1.2 guards: the materiality-cap directive stays in the prompt, analyze() unloads the model after the report, reduce_findings dedupes (incl. containment)/caps/ranks by forensic weight + honest empty — 15 tests. |
| `tests/test_wealth_lock.py` | The Gold-ETF paper sweep + the merged flywheel, offline (':memory:' ledgers, Discord patched, hermetic scrip-report fixtures — the sizing gate is never decided by whatever sits in the gitignored data/ folder): half-of-profit math, losses/breakeven never sweep, idempotent per journal_ref, whole-unit sizing with honest residual, sub-one-unit earmarks accumulating instead of rounding, the ID GATE blocking sizing on mismatch AND on every unverifiable state (missing/stale/unavailable/absent row) while the earmark still survives, the card stating a skipped sizing out loud, the pytest network muzzle, and the end-to-end settlement path through portfolio_manager. |
| `tests/test_scrip_master.py` | The scrip clerk offline (tiny synthetic master, injected — never the real 27MB fetch): the three verdicts, normalized matching accepting the master's own index spellings, wanted-list exact match + honest empty, de-duped cards that re-announce when a send failed, a clean run firing nothing, and the honesty rule twice — a timeout AND an empty master both report `unavailable` with the id count at 0, never a pass — 9 tests. |
| `tests/test_darling_tiers.py` | The 7-tier grading engine offline: precedence per the approved mechanical definitions (near-zone 5%, momentum via DMAs, losing-volume 20v60, near-stop 1 ATR beats momentum), overextension never grades a Buy (Law 3), Tier-0 ungraded honesty (no levels / vetoed valuation), watch's forensic override even on a deep discount, pins keep dropped-held names visible at their pinned grade (never a manufactured sell from missing data), stale pins clear when the shadow closes, and the family-transition-only card policy (first-grading summary, intrafamily silence) — 9 tests. |
| `tests/test_weekly_recalibration.py` | The Saturday clock offline: dropped+held names pin by failure flavor (rejected → strong_sell, data-lost → ungraded), unheld drops pin nothing, re-passed and closed-out pins clear, carried pins keep their original story, screen outage fail-opens with zero pins — 4 tests. |
| `tests/test_darling_shadow.py` | The darling shadow leg on tiers: entry frame/contract (setup darling_buy, tier stamped, eod_close honesty, 45d time-stop override), 1R-pivot target rule + anomaly refusal, halt-stack respect, near-zone weak_buy never chased, open/same-day dedup, Strong-Sell force-exits (pinned → fundamental_break, graded → strong_sell_tier, weak_sell untouched), legacy darling_ripe autopsy compat, full cycle order (resolve → force → propose) — 9 tests. |
| `tests/test_valuation_scorer.py` | The valuation engine offline: TTM P/E-PEG-P/S math, veto amendments (negative earnings / flat growth — never capped), insufficient-data honesty, winsorized stats (outlier trimmed, zero-variance abstains), sector min-membership else market fallback, sigmoid directionality (cheap<typical<rich), end-to-end run() scoring only queued darlings — 7 tests. |
| `tests/test_dynamic_pricer.py` | Phase 2 offline: ATR/DMA NULL-honesty, volume-spike anchor + anchored VWAP, HVN bands + thin-history empties, confirmed-only pivots (flank bars can't confirm), overextension + abstain, levels_for full/thin, run() writes levels + shadow journal; halt stack — never-short absolute, liquidity fail-closed, expiry-week block (monthly last Thursday), overextension blocks delivery buys but abstain doesn't, 1R absence enforced — 12 tests. |
| `tests/test_bhavcopy_clerk.py` | The bhavcopy clerk offline: NULL-honest parse (' -' → None, series filter), capture + idempotent skip, HTML-error-page → honest holiday no_file, backfill walk skips weekends and survives outages, bars_for chronological + honest empties — 5 tests. |
| `tests/test_fo_bhavcopy.py` | F&O intake offline: bundle ingest (nested zip routing), OPTSTK/FUTSTK aggregation (index F&O excluded, padded numbers), ban parse, tiering + banned exclusion, newest-day-WITH-bhavcopy selection (pre-open ban-only days), fetch_day (zip unwrap, idempotent, html-error honest miss) — 5 tests. |
| `tests/test_integrated_results.py` | The Issue-21 fix clerk offline: context-anchored XBRL parse (OneD not YTD, lakhs conversion, EPS fallback tag), quarter selection (financials only, Consolidated preferred, newest revision wins), fresh-capture assembly in the exact financial_results schema, honest no_data/outage — 5 tests. |
| `tests/test_darling_pipeline.py` | Darling Pipeline Phase 1 offline: nse_results normalization (typed floats, '-'→None, bank flag via interest earned), capture/no-data/outage codes, never-crash loop; screener v1 rule (pass/reject on growth-streak-EPS, banks on interest earned, insufficient-data honesty), forensic gate (reject <45 or ≥2 reds, caution band flags, contaminated originals invisible + .v2 wins), queue write with all passers; downloader reads darlings_queue.json first then legacy screening_queue.json — 12 tests. |
| `tests/test_report_pipeline.py` | The fundamental-research pipeline offline: downloader (newest-PDF-only listing parse, `--fiscal` exact-year row pick + honest-None on a missing year, .NS→NSE symbol, idempotent re-run, non-PDF body → RD-500, dead API → one retry then RD-401, the never-crash loop summary, queue read + honest [] on missing); analyzer (NUL-byte stripping from extract_pages via a mocked PdfReader, ligature+boilerplate-precedence tagging, density-budget condensation + rollover, page-marker chunking, the verbatim-quote validator keep/drop incl. ±1-page tolerance, mechanical flag-heavy score, end-to-end analyze() with a FakeExtractor that hallucinates on purpose — the fabricated finding must be dropped and counted) — 16 tests, offline. |
| `tests/test_margin_stress.py` | The 2026-07-19 SPAN work: `span_stress_factor` band boundaries (16/25) + fail-toward-1.0; `required_margin_for` unstressed when calm/unknown, stressed from the proposal's own VIX, explicit-vix override; the daily circuit breaker's four staging tests (ported on merge: 3% trip incl. the exact boundary, within-budget/profit, equity-abstain, today-only P&L); LIVE gate integration on ':memory:' (a 3.005% settle-day halts new entries + fires ONE de-duped Discord card, small-loss/profit days don't, risk-of-ruin outranks the breaker, IST stamp discipline); the audit's totals/drift-flag/greedy-squeeze-out — 17 tests, offline. |
| `tests/test_analysis_signals.py` | Dept 8 signal modules (closes the rest of the review #2 coverage gap): smart_money_trend value-weighted NIV + STRICT point-in-time boundary (decision-day deal excluded) + block-only/side-filtered VWAP + smart_money_ok honest abstain + load_deals_by_ticker grouping/junk-skip via a tmp ledger; sector_trend bullish above/below both SMAs + NULL-honest short-history error + relative-strength leader/laggard/no-bars/short-history; macro_shocks.active_shock inclusive window boundaries; conviction factor functions (pristine=1.0, value-trap ×0.25 crush, roe-None neutral, sector half-credit) + 40/40/20 weighting + the exactly-0.40-is-not-a-veto boundary; institutional_alpha accumulation thresholds (2-deal minimum, 0.20 net ratio, decision-day exclusion) + pullback_trigger hold/invalidation/from-above/no-vwap; **the IST decision-day fix** (default date from `market_loop.ist_now`, not host tz; explicit `as_of` threads `advise()` → `_distribution`) — 33 tests, offline. |
| `tests/test_regime_filters.py` | Dept 8 manager seam (the review #2 zero-coverage finding, closed 2026-07-19): every crisis trigger (VIX≥25 panic, ≥15% d/d spike, macro-shock window) + calm/None/zero-prev fail-opens; the ≥2-of-top-3 distribution veto incl. the 90d window boundary and no-deals/no-mapping fail-opens; the sector veto with injected `is_sector_bullish` (bearish/bullish/exploding/no-mapping); `advise()` verdict shape + permissive fail-open on a radar exception; the PROPOSER CONTRACT (veto blocks ONLY bullish, crisis blocks ONLY neutral short-premium, permissive advisory byte-identical to none — all via injected analysis/vix/expiry/empty-chain, zero network); `fetch_market_state` composes the advisory and an unreadable deals ledger costs the advisory, never the cycle — 25 tests, offline. |
| `tests/test_ceo_brief.py` | CEO Brief: issue bucketing + most-specific-first precedence, a guard that every bucket pattern matches text `ops_monitor` actually flags (no dead patterns), heartbeat green/silent/fail-open, **the anti-theft pair — the brief's sweep leaves `ops_monitor`'s state intact and never writes the problem ledger**, own-offset advance, corrupt-state recovery, deploy-log latest-per-service + split-sha shout + torn-line tolerance + host stamp, risk reuse of `eod_summary`, card render through the REAL `_build_embed` (colour + 4 fields survive), `fire_broadcast`-only routing, dry-run sends nothing — 37 tests, offline. |
| `tests/test_ops_monitor.py` | Ops sweep: problem-pattern matching, incremental offsets, rotation recovery, dedupe counts, self-log exclusion, weekday-aware heartbeats, jsonl ledger, card formatting, broken-notifier safety — 10 tests, offline. |
| `tests/test_master_scheduler.py` | Phase 7A scheduler: session-window math, past-close misfire exit, loops armed + self-close at 15:30, stop-event (signal) shutdown, pre-open wait cancellation, dying-loop safety, planner playbook lines — 8 tests, offline (hand-wound IST clock). |
| `tests/test_regime.py` | Regime memory: band boundaries, capture at entry creation (live + simulator), schema migrations, NULL-honesty for pre-regime rows, regime-filtered query + backward compat, skeptic v2 feature slots + trainer fallbacks, as-of backfill idempotency — 11 tests, offline. |
| `tests/test_edge_miner.py` | Edge miner: 20h due-gate, Ollama/gcloud skip guards, new-triple diffing (reinforces excluded), full pull→mine→apply→refresh cycle with a fake gcloud runner (asserts scripts ship as FILES, never inline `-c` python over ssh), no-apply-when-nothing-new, failed-pull safety — 7 tests, offline. |
| `tests/test_evolution.py` | Procedural Evolution: cluster mining + provenance, hindsight buckets, counterfactuals, proposal/JSON schema gates, consensus-gate blocks + withdrawals, override_parameters crash-safe restore, portfolio metric math, all three backtest verdicts, lineage version chains, 4-section candidate markdown + real diff, orchestrator end-to-end, sleep-phase graceful skip — 14 tests, offline (scripted fake LLM). |
| `tests/test_resonance.py` | Phase 7 suite: macro matrix (snapshot fallback, mocked Dhan live path incl. DH-906 fail-open), news parser coercion (all Ollama HTTP mocked), resonance verdicts (the bear-put-vs-crude-crash CONFLICT scenario, strike/expiry-roll payloads, horizon blending), mode=ro graph guard — 27 tests, offline. |
| `tests/test_market_snapshot.py` | Phase 8 read-model: atomic write/read, staleness window, live_cycle publishing, the dashboard's zero-Dhan snapshot path + equity/spread mixed ladder + honest mark_source — 12 tests, offline. |
| `tests/test_rules.py` | Alert rule logic, offline. |
| `tests/test_portfolio.py` | Portfolio math + strategy proposals, plus the Phase 6G capital layer (margin lock/exhaustion boundaries, consecutive-loss drawdown scenarios, 10% risk-of-ruin halt, `run_headless` gate integration — in-memory DB). Offline. |
| `tests/test_forecast.py` | Forecast checklist scoring, offline (monkeypatches `suggestions.analyze`). |
| `tests/test_tuner.py` | Tuner weight learning, offline (fake journal entries). |

## Staging (NOT live architecture)

| Folder | Purpose |
|---|---|
| `next_gen_engine/` | Stage 4/5 institutional-maturity build (2026-07-17 Build Day), **local staging only — nothing here is imported by `src/`, scheduled, or deployed.** 6 modules across 4 blueprint phases (risk: GOLDBEES flywheel / ATR trailing stops — the daily circuit breaker MERGED OUT 2026-07-19 into `src/portfolio_manager.py`'s halt list, staging file deleted per the anti-orphan rule; execution: limit-chase + protective-leg sequencing; wisdom: text→backtestable-JSON via `text_intelligence`; scaling: redis EDA + Dhan WebSocket drafts, both with OPTIONAL deps kept out of `requirements.txt`). Each module's header + `next_gen_engine/README.md` name the CANONICAL `src/` merge target (anti-orphan rule). Tests: `tests/test_next_gen_engine.py` (25 hermetic). Move logic INTO the named target at deploy; never let this become a second implementation. |

## Documentation suite (this file's siblings)

| File | Purpose |
|---|---|
| `OVERVIEW.md` | Vision + non-negotiables. Read first. |
| `ARCHITECTURE.md` | System flow diagrams + hosting/state model. |
| `MODULES.md` | This file. |
| `DECISIONS.md` | Log of locked architectural decisions + why. |
| `HANDOVER.md` | Cold-start: credentials, env vars, boot commands, current state. |
| `DATA_CONTRACT.md` | Exact JSON schemas + API contract for the frontend. |
| `docs/SYSTEM_MAP_AND_AUDIT.md` | Evidence-based (import/data-path) Mermaid map + the Orphan/Redundancy audit. Generated 2026-07-16 for the Saturday Fable pre-review (predates Dept 8 — it maps the then-7 departments); flags the `dhan_guard` Manager bypass and the `tuner`/`resonance`/`decay_engine` orphans to resolve before the unified push. |
| `PLAN.md` | Phase 4 (4A-4F) build plan and status — historical build log. |
| `VISION_PLAN.md` | User's Phase 5+ master blueprint (Discord, Brain Map, simulator, event ingestion). |
| `DECISIONS.md`, `PLAN.md`, `VISION_PLAN.md` are historical/planning docs and are NOT rewritten at every milestone the way `OVERVIEW`/`ARCHITECTURE`/`MODULES`/`HANDOVER` are — they're appended to as new phases/decisions land. |
