# Deploy runbook — 2026-07-20 01:00 IST (hold lifted)

**Payload:** 22 commits, `6d89eb4` → `6fa1fd6`. Suite **1299 green**
(2026-07-19 ~21:50). `requirements.txt` UNTOUCHED — zero new pip deps on
the VM (pypdf stays Mac-side by design, lazy-imported only by research
CLIs the VM never runs). No new `.env` keys needed (Gemini key already
on the VM for news_processor). No data migrations.

## What actually changes live behaviour on the VM

1. **Entry-time VIX-stress margin** (`eccfd05`): required margin ×1.15
   at VIX≥16, ×1.30 at VIX≥25, priced identically at proposal AND
   approval. Panic reserves more upfront → fewer trades in storms.
2. **Daily circuit breaker** (`eccfd05`): −3% realized on the IST day →
   no new entries for the rest of that day, ONE de-duped Discord card.
   Composed `ENTRY_HALT_CHECKS` (risk-of-ruin → daily breaker).
3. **IST clock fixes** (`f5b5c42`): regime_filters decision-day +
   portfolio_manager timestamps derive from IST, not host UTC — early-
   morning boundary bug (Issue 16) gone.
4. **CEO brief + EOD card** (`3d9ca02`): new cron entries — crontab must
   be re-synced from `scripts/setup_cron.sh` (step 4 below).
5. Everything else in the delta (research pipeline, darling screen,
   bhavcopy clerk, Gemini backend, next_gen staging) is **inert on the
   VM**: code lands, nothing schedules or imports it in the live loop.

## Steps (copy-paste, in order)

**Mac — push (this lifts the hold):**
```bash
cd ~/Documents/Claude/alpha_trading && git push origin main
```

**Mac — SSH to the VM** (always `adigupta1998@`, never the default):
```bash
gcloud compute ssh adigupta1998@alpha-trading-vm \
  --project=project-37632031-10d0-47dd-b6f --zone=us-central1-a
```

**VM — pull + restart (one block):**
```bash
cd ~/alpha_trading && git pull && \
venv/bin/pip install -r requirements.txt -q && \
sudo systemctl restart alpha-trading && sleep 3 && \
systemctl is-active alpha-trading && curl -s localhost:8000/api/health
```

**VM — re-sync cron (the EOD-card/CEO-brief entries are new):**
```bash
cd ~/alpha_trading && crontab -l > ~/crontab.bak-$(date +%Y%m%d) && \
bash scripts/setup_cron.sh && crontab -l | grep -c "src\."
```
(back up first, then re-apply; the count printed should match the
script's job count — eyeball `crontab -l` if unsure)

**VM — verify the new risk layer actually loaded:**
```bash
cd ~/alpha_trading && venv/bin/python -m src.margin_audit | tail -5 && \
sudo journalctl -u alpha-trading -n 20 --no-pager
```
Expect: margin audit prints its three-part summary (drift/stress/panic);
journal shows a clean start, no tracebacks.

## Rollback (if anything looks wrong)

```bash
cd ~/alpha_trading && git reset --hard 6d89eb4 && \
sudo systemctl restart alpha-trading && crontab ~/crontab.bak-$(date +%Y%m%d)
```

## Reminders
- Token pastes: NEVER paste secrets into the SSH terminal (the
  base64/scanner gotcha) — nothing in this deploy needs one.
- Quick-tunnel URL rotates on restart — refetch after:
  `sudo journalctl -u cloudflared-tunnel --no-pager | grep -o 'https://[a-zA-Z0-9.-]*\.trycloudflare\.com' | tail -1`
- Monday 09:15 IST is the first live session on the new risk layer —
  watch the first proposal's margin line for the stress factor.
