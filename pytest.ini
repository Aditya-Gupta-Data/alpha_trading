[pytest]
; Only collect the live suite. research_archive/tests/ holds frozen reference
; tests for archived modules (their imports point at old src paths on purpose)
; and must never be collected.
testpaths = tests
